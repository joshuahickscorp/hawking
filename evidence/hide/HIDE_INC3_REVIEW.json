{
  "schema": "hawking.hide.inc3_review.v1",
  "at": "2026-07-26T05:55:00Z",
  "status": "VERIFIED BY CLAUDE. 244 passed, 0 failed (was 240). Not merged.",
  "w5_permission_hole": {
    "closed": true,
    "defense_in_depth": "Refused at the intent boundary (host.rs:1235, 'approve_effect requires step_id (blanket approve is refused)') AND again at the drain site.",
    "asymmetry_preserved": "Blanket DENY still works, because a deny with no step_id is fail-safe. This was deliberate in the contract and is the kind of thing a symmetry-loving refactor silently destroys.",
    "tests": [
      "rpc::tests::approval_respond_refuse_approve_without_step_id",
      "rpc::tests::approval_respond_allows_deny_without_step_id",
      "host::tests::approve_effect_without_step_id_while_nothing_pending_does_not_run_effect"
    ]
  },
  "w6": "POST /v1/hide/initialize routed at hide-serve/src/lib.rs:55 reaching BackendHost::initialize.",
  "w7": "hide-state absorbed, not wired. Its module doc now states it is superseded as the live checkpoint authority, and state/save|load|fork|release route onto the host's existing checkpoint_* family. One authority, not two.",
  "hide_line_after_four_increments": [
    "MCP boot-registered", "fleet dispatch live", "merge funnel wired",
    "real SQLite index at workspace open", "approval round-trip closed with the deny trap pinned",
    "permission hole closed", "initialize route wired", "hide-state absorbed"
  ],
  "not_done_deliberately": "HIDE_KERNEL_TURN default NOT flipped. Its stated prerequisites are now genuinely met, but flipping it is a human decision, not a consequence of tests passing.",
  "blocking_structural_fact": "All of this lives on the HIDE line, not the campaign branch. Merging the trees is a prerequisite for Fabric work -- and must NOT be done while PASS3 is running out of the campaign working tree, since spawned pack workers re-import from disk.",
  "evidence_level": "TESTS_RE_RUN_INDEPENDENTLY"
}
