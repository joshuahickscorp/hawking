# dismantle roadmap — state snapshot 2026-05-30

Single-page "where are we" so a fresh session can orient without re-treading.
Authoritative strategy is `plans/throughput_bible_2026_05_30.md`; the queued
work is `plans/staging_session_worklist_2026_05_30.md`; killed levers and their
resurrection checks are `reports/dead_levers.md`.

Headline: Qwen-3B-Q4_K_M decode on M3 Pro. Decode is **kernel-bound (~85% GPU
busy)**; the gap to llama.cpp ~50 tps is **bandwidth efficiency** (~41% of peak
vs ~60%), not parallelism or dispatch overhead.

---

## ✅ Shipped (on this branch / main)

- **path-to-50 Q4_K predec, +32.5% decode, bit-identical** — `2r` 2-row-ILP
  default-on, fused gate+up `predec_pair` (one dispatch for both FFN projections),
  ffn_down routed through predec. Commit `d2343cc`. ~24 → ~32 dec_tps.
- **Earlier predec default-on (+30.9%)** — `6f0209e`, first sub-2× gap to llama.
- **§1 methodology gate** — `analyze_tcb_trace.py` refuses physics-violating
  traces (INV1 busy-BW ≤ peak, INV2 unmapped < 5%, INV3 token-count from argmax,
  INV4 bit-identical parity). All decode kernels now mapped (`41caa29`).
- **Reusable paired-bench harness** — `tools/bench/paired_lever.sh` (`fe065f3`).
- **Q4K_FAST (+28%, held)**, **vocab-prune-32K**, **Q4K-LM-head**, **batched
  prefill (2.1–2.3×)** — see MEMORY.md.

## 🪦 Dead / held (do not re-spawn — see reports/dead_levers.md)

- **Host-side per-dispatch overhead family** — ICB, concurrent QKV encoder
  (+1.68%), PSO-transition batching (1.06×), CPU-encode (0.51%). The 36 ms/token
  "gap" is **real GPU-side**, not host. Kernel BW efficiency is the lever.
- **W4A8 / mixed-precision as default** — quality 20% bit-identical (N=100),
  perf 1.115× (below 1.20× gate), sub-additive with predec. Held behind a flag.
- **Predec `_4r`** — speculative, motivated by a rejected trace; unvalidated,
  opt-in only. Gated on 0.1 profiling.
- **FFN block-256 sparsity**, **MLA Phase 4**, **Eagle5 v1 routing-mask**,
  **f16 residual**, **MoE megakernel/serial dispatch**, **Q5_0 simd_shuffle**,
  **LM-head simdmat as a tps lever** — all with killing evidence in the registry.

## 🔧 In-flight (active, another session / cloud)

- **EAGLE spec-decode** — runtime wiring is **FIXED** (`4384f68`): the layer-32
  residual/intermediate feed used to be gated behind the corpus-dump flag, so
  plain `--speculate eagle5` fed the head ZEROS → garbage drafts → 0% accept.
  Now feeds real residuals (cosine-1.0 vs capture), accept 0→~10.5% (K=2),
  bit-identical no-spec. **The remaining gap is HEAD GENERALIZATION, not the
  runtime:** the head OVERFITS its 619-seq corpus (90% depth-0 in-sample vs
  **33% held-out**, 13.6% live) — the earlier τ=1.33→1.91 were in-sample and
  misleading. Fix = retrain `num_blocks=2` on a **bigger/more-diverse** Q4_K_M
  capture; nb02 now holds out 20% so the τ gate measures generalization not
  memorization (`cc260ee`). A 2500-prompt capture is running locally; cloud
  retrain fires after. Handoff: `plans/eagle_forward_parity_handoff.md`
  (RESOLVED banner). Spec stays net-negative on tps until the head generalizes.
- **Byte-cut (Q3_K)** — quality viable (+4.7% PPL, −11% bytes from a clean
  llama.cpp Q8_0 source; the oracle-C Q3=5.9 was a requant-from-Q4 artifact).
  The fast-GEMV blocker is now **scaffolded**: `gemm_q3_k_v4_predec` +
  `gemv_q3_k_v4_predec_pinned_tcb` landed (`ec8f5bb`), bit-identical to
  `gemm_q3_k_fused_v2` by construction (host pre-decodes the same `d*scale`
  product), offline-MSL-validated (compiles to AIR). **Remaining = on-GPU
  parity test + paired bench + forward-pass wiring** (bench-gated; a Q3_K model
  still loads the generic path until wired).

## 📋 Queued (staging worklist — scaffold now, bench when GPU frees)

Three non-blocking lanes: **CPU-scaffold** (stubs + gates + parity skeletons +
bench entries), **GPU-bench** (serialized paired bench, 0.1-profiling-ranked),
**cloud** (AWQ/EAGLE). Highest-confidence: **1.1 prefill MMA** (+10–20%).
**1.6 Q3_K** kernel is now scaffolded (`ec8f5bb`, see In-flight) — next GPU-lane
item is its parity + bench. **f16-predec-scales GEMV** (`_2r_f16s`, Stage-2
bandwidth lever — cuts the predec scale table 192→160 B/block; kernel written,
uncommitted, atol-1e-3 not bit-identical) is queued behind a quality gate.
Biggest ceiling: **1.7 simdgroup-matrix decode** (MLX-class, ~41%→60–80% of
peak) — gate it on **0.1 xctrace profiling** so it targets the real stall
instead of guessing (the `_4r` lesson). See
`plans/staging_session_worklist_2026_05_30.md` for the full Tier 0–4 list.

## 🧭 Lessons that change how we work

- **In-sample ≠ held-out (EAGLE).** A draft head scoring 90% on its own
  training corpus scored 33% held-out / 13.6% live. Any acceptance/τ number
  from the training corpus is meaningless for ship decisions — always eval on a
  held-out split (nb02 now enforces this). Applies to every learned component.
- **Validate kernels offline before claiming "compiles."** `xcrun metal -c` on
  the concatenated shader source (common+quant+… → AIR) catches MSL syntax
  errors on the CPU, no GPU needed. "Bench/parity GPU-gated" ≠ "syntax unknown."
- **TCB hides per-kernel timing from Instruments.** TokenCommandBuffer bundles a
  token's kernels into one command buffer, so a Metal System Trace sees
  per-command-buffer, not per-kernel, intervals. The homemade TCB trace is
  split-CB-distorted (the §1 gate rejects it). Per-kernel busy-time needs the
  `mst_export.sh` → `mst_analyze.py` path on a real `.trace` bundle.
- **Don't add accumulator chains to a bandwidth-bound GEMV.** `_4r` (4-row ILP)
  was ~−1% vs `_2r`: 2 chains already saturate the bus. Stage-2 levers must cut
  wasted bandwidth/instructions (vectorized unpack, coalesced layout, f16
  scales), not add register pressure.
