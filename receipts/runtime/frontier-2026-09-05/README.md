# Frontier measurement set, 2026-09-05

Evidence and the exact method that produced it, moved off the repository root.

    wall_g001.jsonl        WorkUnit wall decomposition: total 549.959 s, of which
                           resident 547.362 s (99.5%), persistence 0.0016 s,
                           context_compile 0.0002 s
    wall_frontier.jsonl    the same for the frontier unit: total 1030.108 s,
                           resident 1027.545 s, tools 0.0068 s
    drive_rounds.jsonl     one escalating-driver round: 569.5 s, structured output
                           rejected after 3 attempts on an empty response

    mission_*.txt          the goal texts that produced those runs
    run_*.sh               the runners
    _round_prompt.txt      the driver's round prompt

These are the numbers behind the standing claim that the complete WorkUnit wall is
essentially one resident call. Kept because a measurement without its method is not
reproducible, and evidence cannot be regenerated by rerunning a build.
