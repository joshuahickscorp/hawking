{
  "schema": "hawking.gravity.bf16_native.v1",
  "at": "2026-07-26T20:10:00Z",
  "status": {
    "implementation": "BUILT",
    "default_path": "UNTOUCHED (flags default off; host dense remains oracle)",
    "metric_unit_tests": "PASS (numeric_parity 11/11 + gravity_glm flag/byte tests)",
    "device_parity_verdict": "AWAITING_CONTROLLER_DEVICE_RUN",
    "byte_time_ledger": "AWAITING_CONTROLLER_DEVICE_RUN",
    "sandbox_note": "No Metal device in executor sandbox. No TPS, live active-byte, or device parity verdict claimed."
  },
  "flag": {
    "primary": "HAWKING_GLM_GPU_LM_HEAD=1",
    "full_logits_opt_in": "HAWKING_GLM_GPU_LM_HEAD_FULL_LOGITS=1",
    "resident_optional": "HAWKING_GLM_GPU_RESIDENT_STATE=1",
    "default": "all off — host path is the parity oracle"
  },
  "what_became_resident": {
    "when_flag_on": "Every rank-2 native.bf16 matvec target under GpuWeightCache",
    "tensors": [
      {
        "name": "lm_head.weight",
        "stored": "native.bf16 [154880, 6144] = 1.90 GB",
        "upload": "once (raw bf16, no host widen)",
        "per_token_weight_traffic": "1.90 GB active (stored bf16), not 3.81 GB f32"
      },
      {
        "name": "indexer projections (wq_b, wk, weights_proj) on full-indexer layers",
        "stored": "native.bf16",
        "upload": "once per tensor under weight-cache budget",
        "widen_tax_removed_static_gb": 0.394
      },
      {
        "name": "router gate.weight on sparse layers",
        "stored": "native.bf16",
        "upload": "once per tensor under weight-cache budget",
        "widen_tax_removed_static_gb": 0.236
      }
    ],
    "still_host_dense": "norms, biases, embedding rows (dense()/row); non-rank-2 natives; PQ experts"
  },
  "what_still_moves_per_token": {
    "weights_default_off": "host f32-widened matvec streams (lm_head 3.81 GB + other native widen tax)",
    "weights_flag_on": "device streams stored bf16 for resident natives; first-touch upload only on cache miss",
    "activations": "final_hidden stays on device for resident lm_head path",
    "logits_default_device_path": "NOT read back (empty host Vec); token + top-5 diagnostics only (~44 B)",
    "logits_full_opt_in": "154880 f32 when HAWKING_GLM_GPU_LM_HEAD_FULL_LOGITS=1 (~620 KB)",
    "token_diagnostics": "u32 sample_token + 5×(u32 idx + f32 val)"
  },
  "kernel": {
    "name": "gemv_native_bf16_seq",
    "shader": "crates/hawking-core/shaders/matmul.metal",
    "input_dtype": "bf16 (ushort bit patterns, row-major)",
    "accumulator": "f32 sequential left-to-right (fp contract off; no FMA reassociation)",
    "widen_rule": "((uint)bf16_bits) << 16 → as_type<float> — same as gravity::widen_native",
    "launch": "one thread per output row; TG 256",
    "sampling": {
      "argmax": "sample_argmax_f32 (greedy token)",
      "topk_diag": "sample_topk with k=5 (GPU_LM_HEAD_DIAG_TOPK)"
    }
  },
  "v2_1_metric_table": {
    "canonical": "NUMERIC_PARITY_V2_1.md",
    "harness": "crates/hawking-core/src/numeric_parity.rs",
    "authority": "FP64 matvec_bf16_f64_authority — not the f32 host",
    "metrics": [
      {
        "name": "absolute_error_near_zero",
        "gate": true,
        "cutoff_rule": "max(1e-30, 1e-6 * median(|reference|))",
        "bound": "max_abs_near_zero ≤ 1e-4"
      },
      {
        "name": "relative_l2_full_vector",
        "gate": true,
        "headline": true,
        "bound": "≤ 1e-5"
      },
      {
        "name": "meaningful_scale_max_relative",
        "gate": true,
        "bound": "≤ 1e-5 only where |ref| ≥ cutoff"
      },
      {
        "name": "ulp_distribution",
        "gate": false,
        "report": "median / p95 / p99 / max — never a lone max"
      },
      {
        "name": "cosine_similarity",
        "gate": true,
        "bound": "≥ 1 − 1e-7"
      },
      {
        "name": "kl_softmax",
        "gate": true,
        "bound": "≤ 1e-6 nats"
      },
      {
        "name": "exact_topk_and_greedy",
        "gate": true,
        "tolerance": "NONE",
        "top_k": 5
      },
      {
        "name": "diagnostic_max_scalar_rel_all",
        "gate": false,
        "note": "V2 scalar retained only to show why the prior rejection misfired"
      }
    ],
    "prior_rejection": "NUMERIC_GATE_INSUFFICIENTLY_CONDITIONED (max scalar rel on denormal-scale span)"
  },
  "static_byte_expectation_not_measured": {
    "label": "AWAITING_CONTROLLER_LEDGER — not a measured verdict",
    "lm_head_active_when_flag_on_gb": 1.903,
    "lm_head_active_when_flag_off_gb": 3.806,
    "native_widen_tax_removed_if_all_natives_device_gb": 2.533,
    "combined_surplus_share_targeted": "6.34 / 6.76 GB ≈ 94% of static surplus (static schedule, not live)"
  },
  "commands": {
    "metric_unit_tests_no_metal": "cargo test -p hawking-core --lib numeric_parity -- --nocapture",
    "flag_and_byte_unit_tests": "cargo test -p hawking-core --lib gravity_glm:: -- --nocapture",
    "device_parity_v21": "cargo test -p hawking-core --test gravity_glm_lm_head_device_parity -- --nocapture",
    "resident_default_still_green": "cargo test -p hawking-core --test gravity_glm_resident_parity -- --nocapture",
    "byte_time_ledger_device_path": "HAWKING_GLM_GPU_LM_HEAD=1 HAWKING_GLM_GPU_RESIDENT_STATE=1 HAWKING_COST_LEDGER=1 cargo run --release -p hawking-core --example gravity_glm_cost_ledger -- --context 4 --warmup 40 --ledger-tokens 20 --out reports/base_runtime/tg_bf16_native_ledger.json",
    "byte_time_ledger_oracle_path": "HAWKING_COST_LEDGER=1 cargo run --release -p hawking-core --example gravity_glm_cost_ledger -- --context 4 --warmup 40 --ledger-tokens 20 --out reports/base_runtime/tg_oracle_ledger.json",
    "continuous_logit_parity_on_forward": "HAWKING_GLM_GPU_LM_HEAD=1 HAWKING_GLM_GPU_LM_HEAD_FULL_LOGITS=1 HAWKING_GLM_GPU_RESIDENT_STATE=1 cargo test -p hawking-core --test gravity_glm_lm_head_device_parity -- --nocapture"
  },
  "isolation": {
    "default_resident_path": "untouched when flags off",
    "tools_prometheus": "untouched",
    "mop": "untouched",
    "sealed_math_preserve": "untouched"
  }
}
