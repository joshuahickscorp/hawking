# Kimi K2.6 handoff precheck

The authoritative repository began at `2e968ad602b3e951e68b76da1bac8e57f5f00949` on `campaign/storage-stripdown-resident-first`; fetched `origin/main` was `4fbca8bc6d33117707933793bb3293d9463c049f`. The required `campaign/kimi-k26-doctor-prime` branch was created without discarding or rewriting any pre-existing working-tree changes.

MOP resolves exactly to `/Users/scammermike/Downloads/mop` on device `16777233`, inode `1233332`. It and its active Telegram agent were observed and left untouched.

The Kimi cache existed, but actual-file inspection found only `config.json` (5,416 logical bytes; 12 KiB allocated for the cache root). It is therefore `PARTIAL_RESUMABLE_LOCAL`, not a complete source. The authoritative one-copy destination is that same content-addressed Hugging Face cache.

The interim DeepSeek-V4-Flash-DSpark source had already had its 48 raw shards released by the immediately preceding storage campaign. Its immutable revision, license, index, tokenizer, code, logical ledger, and release receipts remain; only 24,228,571 metadata bytes remain at its source path.

At precheck there was no live model or Apple-heavy process. HIDE had an active local dev server rooted in `/Users/scammermike/Downloads/hawking-hide-build/target`; its modified source tree was preserved, its dev process groups were stopped cleanly, and only that rebuildable `target` tree was removed after an open-file check. A stale Qwen notifier watching a dead PID was also stopped. No MOP, document, credential, application-data, or unrelated source tree was cleaned.

The machine has 103,079,215,104 bytes of RAM. Initial physical free disk was 617,159,221,248 bytes, with `df` and APFS container accounting reconciled; purgeable space was not used to pass the gate.
