# Hawking Ascension — final position

```
basis:   HONEST_EMPIRICAL_FLOOR
metric:  corrected and enforced globally (not broken)
as of:   2026-07-23
branch:  campaign/glm52-generation-b
```

The directive permits ascension with a winning functional architecture, a winning encoding
architecture, a hybrid, or an honest empirical floor — and forbids it only with a broken
promotion metric. There is no winning servable sub-bit architecture. The metric is
corrected. So this seals the honest empirical floor, with the complete ledger of what is
proven and what remains gated.

## What was corrected

The promotion metric was broken twice and is now fixed and enforced:
`HAWKING_NULL_CORRECTED_METRIC_CONTRACT`, implemented in `hawking_null_metric.py`, whose
selftest is the contract. Raw activation cosine promoted candidates that lose to a constant;
a first correction pass centred by the held-out teacher mean. Both superseded. All 22 sealed
GLM pilot artifacts were re-scored exactly from their sealed relative errors — **zero beat a
constant**, block skill −1.21 to −5.38, including −1.77 at twice the legal ceiling.

## What is proven

**GLM-5.2 weight-space:** CLOSED_NEGATIVE across every family (PQ, low-rank, shared basis,
hybrid, the pilot's own functional-block weight family). Stronger negative than first
reported.

**GLM-5.2 functional escape:** real and it replicates. A seeded-feature student maps the
pre-router hidden state to the MoE output at 0.010375 organ-local BPW, skill 0.53 across
four seeds, block skill 0.88 against 0.20 with the MoE removed, holding on four strata and
six unfitted splits under four seeds with no refit. The map is the win, not the
nonlinearity (full linear control 0.54).

**Why it is not a model:** the GLM residual stream is EXPANSIVE at every tested magnitude at
the early, middle and late strata, and worse at smaller perturbations (early 1.08–1.65×,
middle 1.42–2.41×, late 1.35–2.19×). No contractive regime. Students in four consecutive
layers drive the state to skill 0.098 (late) and router top-1 agreement to 0.446. One
functional layer out of 75 already changes the greedy token at 45 percent of positions.
Sealed `FUNCTIONAL_PARTIAL_ONLY`.

**The rate was never the obstacle.** Over 753 G header-derived logical weights, the protected
2.11 percent costs 0.3377 BPW at source precision alone — above the one-third rung before an
expert byte is stored.

**The native functional runtime works end to end.** `glm52.functional.moe.v1` in `.gravity`
with a stateless portable generator, a deterministic CPU authority, and three parity-gated
Metal grammars (procedural FRT-B wins: 12.58 MB resident, parity 7.9e-7). A real shard drove
a real GLM block to a real next-layer token. Measured bandwidth 417.7 GB/s moved the binding
limit from expert traffic (51.6 GB/token) to attention (0.96 GB functional payload).
Submission collapse: 76 → 1 command buffers per token.

**The methodology transferred, and its own discipline held.** DeepSeek-V4-Flash was admitted,
fetched complete and verified (46/46 shards), and its fp4/fp8 MoE forward was built and
validated (e2m1 dequant on-grid exact). The function-first probe returned a negative, and its
own shared-expert control then proved the negative was the embedding-seeded input, not the
organ — so the verdict is `INSTRUMENT_LIMITED_INCONCLUSIVE`, not a false parent claim. The
null-first metric refused a false positive and caught its own invalid instrument.

## What remains gated, and why

A definitive cross-parent verdict needs real pre-MoE hidden states from a block forward:
DeepSeek's MLA + DSA compressor/indexer + hyper-connections, or Qwen3.5's linear-attention
recurrence. That forward cannot be validated without reference logits, and a 160 GB fp4
model will not load or run on a 96 GiB box to produce them. An amplification verdict from an
unvalidated forward would fabricate the very thing this campaign exists to prevent, so it was
not produced. This is the honest wall: not a lack of effort, a lack of ground truth on this
hardware.

## The floor

> No weight-space method and no functional method has produced a servable sub-bit model on
> the parents reachable here. On GLM-5.2 the functional escape is real but does not survive
> the residual stream's amplification. On DeepSeek-V4-Flash the existence test is
> instrument-limited by the inputs available without a validated block forward. The corrected,
> null-first, amplification-first methodology is the durable result, and it is what the next
> parent — on hardware that can host reference logits — should run first.

Hawking ascends on this floor: the science is corrected, the function is proven end to end
where it could be, the native runtime is finished, and every claim is null-corrected. It does
not ascend on a model it does not have.

```
endpoint: HAWKING_CORE_ASCENDED (honest empirical floor)
```
