{
  "schema": "hawking.glm52.activation_aware_metal_runtime.v1",
  "at": "2026-07-27T15:34:36Z",
  "base_commit": "74a18cc8b44ae0da509869f985e7c18befd15fa9",
  "supersedes_remaining_limit_in": "GLM52_ACTIVATION_AWARE_RUNTIME_ABI_AUDIT_20260727.json",
  "scope": "synthetic CPU/Metal runtime implementation and verification only; no live-pack output, pack log, MOP state, or sealed artifact was read or changed",
  "implemented": {
    "container": [
      "Rust opens model.activation_aware.index.json and model-*.aap lazily",
      "bounds, contiguity, schema, shapes, dispositions, payload magics, source dtypes, and shared-basis geometry fail closed",
      "verified open binds each .aap file to the assembler's shard_sha256",
      "the already-running dtype-omitting pack is decoded from the index's measurement-derived tensor_dtypes"
    ],
    "cpu_codec": [
      "input-side W@x = L@(B.T@x)",
      "output-side W@x = B@(L@x)",
      "row lookup executes factors directly",
      "pass-through BF16/F16/F32 widens through the existing native codec"
    ],
    "metal_codec": [
      "activation_aware_project_f16 computes the rank latent",
      "activation_aware_expand_f16 computes output rows",
      "the two dispatches are ordered in one token command buffer",
      "routed matvec_batch encodes activation-aware calls together",
      "resident-state generic matvec and expert-wave encoding accept activation-aware tensors"
    ],
    "residency": [
      "coefficients are cached under the existing 32 GiB bounded weight LRU",
      "shared bases are SHA-256 deduplicated across tensors and source shards",
      "the distinct-basis cache has a hard 1 GiB fail-closed cap"
    ],
    "engine_route": [
      "GravityEngine resolves assembled activation-aware directories and indexed .aap entries",
      "the normal model registry dispatches .aap to the Metal-only Gravity engine",
      "tokenizer and chat template use the assembler-installed tokenizer directory"
    ]
  },
  "evidence": {
    "python": {
      "command": "python3.12 -m pytest -q tools/condense/tests/test_glm52_activation_aware_runtime.py tools/condense/tests/test_glm52_activation_aware_pack.py",
      "result": "11 passed in 1.61s"
    },
    "rust_core": {
      "command": "cargo test -p hawking-core --lib -- --test-threads=1",
      "result": "242 passed, 5 explicitly ignored, 0 failed",
      "new_proofs": [
        "CPU factorized input and output matvec equal dense authority",
        "CPU factorized row lookup equals dense authority",
        "verified .aap open attaches shared bases and decodes pass-through tensors",
        "Metal two-stage input and output matvec equal the host factor authority",
        "GpuWeightCache loads .aap and uploads one identical shared basis for two tensors",
        "engine resolver recognizes assembled activation-aware directories"
      ]
    },
    "clippy": {
      "command": "cargo clippy -p hawking-core --lib --no-deps -- -D warnings",
      "result": "PASS",
      "note": "workspace-dependency clippy warnings outside hawking-core were excluded with --no-deps and were not modified"
    },
    "diff_check": "PASS"
  },
  "aborted_heavy_check": {
    "command": "cargo test -p hawking-core --test gravity_engine_registry",
    "passed_before_abort": [
      "gravity_detection_reads_magic_not_the_extension",
      "registry_serves_a_gravity_artifact_end_to_end"
    ],
    "aborted_test": "registry_serves_a_multi_shard_glm_artifact_end_to_end",
    "reason": "after about seven minutes, read-only process inspection showed the existing integration consuming about 96 percent CPU and 24 percent memory; that made it a heavy run outside this light-only lane",
    "action": "sent interrupt to the owned test session and confirmed cargo plus child process were gone",
    "claim": "the aborted test is NOT claimed as passing"
  },
  "default_off_specialized_paths": {
    "compact_mla": "PQ-specific and still rejects non-PQ tensors when explicitly enabled",
    "device_dsa": "depends on compact MLA and remains default-off",
    "device_expert_table": "its indirect descriptor ABI supports PQ/native BF16 only; activation-aware falls back to the generic resident/expert-wave path",
    "replay_graphs": "fixed PQ/native projection replay does not claim activation-aware support",
    "meaning": "ordinary GPU, batched routed, resident generic, and expert-wave execution are implemented; specialized default-off PQ graphs are not silently reinterpreted"
  },
  "claims_not_made": [
    "no whole-model activation-aware forward has run because the pack is incomplete",
    "no whole-model Python-versus-Rust logits parity has run",
    "no whole-model speed or sustained decode gate has run",
    "no sealed artifact has run",
    "TEMPORAL_GRAVITY_ASCENT_COMPLETE is not reached"
  ]
}
