# GLM52 activation-pack generation archive

`glm52_activation_aware_pack_v2.py` was a duplicate, registry-only science
generation with no live caller or wrapper. The active GLM52 pack owner is
`glm52_activation_aware_pack.py` (v1), which remains in place with its current
tests and runtime path.

The v2 implementation and dedicated test were removed from active HEAD. Git
history preserves the implementation; no qualified artifact, receipt, or
current pack path was removed.
