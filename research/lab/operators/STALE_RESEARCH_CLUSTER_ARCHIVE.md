# Stale research-cluster archive

These standalone research operators had no live caller, wrapper, worker, or
current receipt owner at HEAD. Their tests and inventory entries were the only
active references. Their implementations remain recoverable in Git history;
current DeepSeek stream execution and Qwen activation-weighted capture paths
are separate and remain intact.

| retired surface | preserved boundary |
| --- | --- |
| `dsv4f_tensor_schedule.py` | manifest-only organ classification; no forward or artifact write |
| `dsv4f_residual_composition_oracle.py` | residual-composition planning oracle; no active execution path |
| `residual_teacher_admission_gate.py` | fail-closed residual-teacher admission semantics |
| `qwen_ascension_preflight.py` | metadata-only preflight decision boundary |

The dedicated tests were retired with the uncalled implementations. No
qualified artifact, physical measurement, or promotion claim was removed.
