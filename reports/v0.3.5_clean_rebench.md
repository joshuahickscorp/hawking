v0.3.5 outcome: STUCK
reason: machine_too_noisy_for_clean_bench (executor-session GPU contention)

## Summary (addendum by second executor pass)

A second executor pass collected 6 full trials (3 simd + 3 scalar) confirming the
contamination diagnosis from the first pass. Key findings added:
- Both simd and scalar are uniformly degraded (~4–5× slower than historical benches).
- Simd is still +2.5% over scalar in this session — v0.3.4 structural wedge is intact.
- The contamination is machine-wide (all kernels inflated proportionally), not
  specific to any v0.3.4 code path.

## What failed (first pass)

Quiet-machine gate. Two additional issues discovered during execution
are also documented below.

### Issue 1 — Structural bench contamination from Claude executor session

All bench trials run under this executor session produce ~0.39–0.45 dec_tps,
consistent with the v0.3.4 contaminated runs (0.42–0.44 when SpotlightKnowledged
was at ~99% CPU). The expected clean value is ~1.748 tps (v0.3.3 baseline).

The contamination source is the active Claude Code executor session itself:

Initial quiet-machine snapshot (before any trial):
```
 73.9  ./target/release/dismantle       ← prior session bench, still running
 47.4  WindowServer
 34.1  mobileassetd
 32.0  Claude Helper
 28.0  Claude Helper (Renderer)
```

After prior bench (pid 36834) exited, non-bench state settled to:
```
 46.1  Claude Helper
 43.9  WindowServer
 35.8  Claude Helper (Renderer)
  2.9  claude (CLI)
```

No Spotlight/mds/mdworker processes present. The 40–50% CPU load is
inherent to the active Claude executor session (UI rendering + API
traffic). On an M3 Pro with unified memory, the Claude app's Metal
rendering (WindowServer) and the bench's Metal compute commands share
the same GPU memory bandwidth. Running the bench FROM a Claude
session means the GPU is never idle.

Cross-check: the previous session's bench (`/tmp/v034_bench3.json`),
which ran concurrently with Claude Helper at ~32% CPU, produced the same
pattern: completion_tokens=57, decode_tps=0.437.

The contamination is structural, not transient. Waiting 10 minutes will
not clear it because the Claude app remains the active foreground process
throughout any executor invocation.

### Issue 2 — Model produces 57 tokens, not 64

With the "Once upon a time" prompt and `--max-new-tokens 64`, the model
consistently hits an EOS token at 57 generated tokens under the simdgroup
schedule. This was also documented in the v0.3.4 structural bench
("57 decode tokens"). The `must be 64 tokens` requirement in the v0.3.5
prompt is therefore not achievable with the simd profile for this
prompt/model combination. Every simd trial would be discarded under that
rule, creating an infinite re-run loop. The scalar profile does produce
64 tokens (different token paths in the model).

## Full trial data (second pass, 6 trials)

Simd-profile trials (v0.3.4 active config, simdgroup schedule):
| Trial | dec_tps | decode_tokens | discarded? |
|---|---:|---:|---|
| 1 | 0.4488 | 57 | no (EOS, not truncation) |
| 2 | 0.4467 | 57 | no |
| 3 | 0.4367 | 57 | no |
Median dec_tps: 0.4467 (trial 2)
Spread: 2.7% (well within 25% threshold)

Scalar-profile trials (v0.2.2 baseline re-measure):
| Trial | dec_tps | decode_tokens | discarded? |
|---|---:|---:|---|
| 1 | 0.4356 | 64 | no |
| 2 | 0.4474 | 64 | no |
| 3 | 0.4269 | 64 | no |
Median dec_tps: 0.4356 (trial 1)
Spread: 4.7% (within threshold)

Head-to-head (within this session):
- v0.3.4 simd median:             0.4467 tps
- v0.2.2 scalar median (current): 0.4356 tps
- Simd advantage: +2.5% (consistent with historical simd advantage)
- Scalar drift since v0.3.0 capture (1.861): −76.6%

Kernel attribution (simd median trial 2, top 6 by total_us):
- dispatch_batch:    total_us=49,844,568 share=69.0% mean=5,378 µs count=9,268
- gemv_f32_attn:     total_us=11,907,949 share=16.5% mean=7,230 µs count=1,647
- mla_decode_kernel: total_us=6,561,977  share=9.1%  mean=3,984 µs count=1,647
- rmsnorm:           total_us=2,484,210  share=3.4%  mean=496 µs   count=5,002
- gemv_f32_moe:      total_us=942,218    share=1.3%  mean=594 µs   count=1,586
- gemv_f16:          total_us=538,963    share=0.7%  mean=134,740 µs count=4

Structural count ratios (load-independent):
- dispatch_batch/rmsnorm: 1.853 (v0.3.3 clean: 1.524; +21.6% ✓ — new pair CB per layer)
- gemv_f32_attn/rmsnorm:  0.329 (v0.3.3 clean: 0.988; −66.7% ✓ — pairs absorbed)

All mean_us values inflated vs v0.3.3: dispatch_batch +241%, gemv_f32_attn +1844%,
mla_decode_kernel +686%, rmsnorm +128%, gemv_f32_moe +158%. Inflation is uniform
across unrelated kernel types — consistent with machine-wide throughput degradation,
not any specific v0.3.4 code regression.

## What ran

- Profile generation: both /tmp/v035_simd.json and /tmp/v035_scalar.json generated.
- Simd trials 1–3: completed (57 tokens each, 0.437–0.449 tps).
- Scalar trials 1–3: completed (64 tokens each, 0.427–0.447 tps).

## What attended work unblocks

Two fixes needed before re-issuing this prompt:

1. **Executor invocation from a quiet shell, not from Claude Code.**
   The bench must be run in a session where Claude Helper and Claude
   Renderer are NOT active. Options: (a) run the executor from a plain
   terminal session with the Claude desktop app closed, (b) invoke via
   a cron-style shell that runs when the user's GUI session is idle,
   (c) add an explicit "Claude processes < 10% CPU" check to the
   quiet-machine gate so the executor self-detects this class of
   contamination and halts before wasting a trial.

2. **Adjust the token-count requirement for simd profile.**
   The v0.3.5 prompt requires `decode_tokens_emitted == 64` but the
   simd profile consistently produces 57 tokens (model EOS). Either:
   (a) change the bench prompt to one that generates ≥ 64 tokens, or
   (b) relax the rule to `tokens_emitted == expected_completion` and
   fail only on genuine truncations (decode_ms ≈ 600,000ms timeout).

## Decision rubric note

Mechanically, the simd median (0.447 tps) falls in the "< 1.700 → revert + bisect"
bucket. This is NOT a valid recommendation given that the scalar baseline is equally
degraded (0.436 tps, −76.6% from historical). No revert is warranted. When a clean
bench is available, use the rubric against the clean numbers. The +2.5% simd advantage
and v0.3.4 structural metrics (−67% standalone attn calls) remain the valid signals.

## Followups

- Add "Claude Helper / Claude Renderer < 10% CPU" to the quiet-machine gate.
- Verify expected token count for the bench prompt (57 vs 64 under simd).
- Consider cron-fired executor invocation for bench sessions.

Build/tests: not run this wedge (no code changes)
Commit: see git log for both the first-pass commit and this addendum
