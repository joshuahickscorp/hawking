# dismantle v1.0.0 Close Report

Date: 2026-05-12

## Phase Outcomes

- Phase 1: `2e4ea9a` partial. Landed `ExpertCache`, per-layer expert stats,
  advisory `posix_madvise` primitives, `--max-routed-expert-ram-mb`, stats
  diagnostic, and V2-Lite no-op parity. Full DeepSeek route-id wiring was
  skipped because single-TCB route IDs remain inside the reused Metal arena.
- Phase 2: `171b44d` partial. Landed Mixtral architecture detection,
  tokenizer scaffolding, preview `MixtralEngine`, synthetic shape smoke, and
  `docs/mixtral.md`. Real Mixtral smoke skipped: no Mixtral GGUF in `models/`.
- Phase 3: `04e4548` removed broken path. `DISMANTLE_RESIDUAL_DTYPE=f16`
  reproduced garbage output (`仅从` loop); the opt-in now errors clearly with
  "F32 only". README updated for v1.0.0 framing. Clippy passes with current
  lint debt explicitly scoped.
- Phase 4: close and final bench. Report plus final coexist measurement.

## V2-Lite Bench

Starting coexist reference: 19.5 dec_tps.

Final command:

```bash
TRIALS=6 TOKENS=64 bash tools/bench/coexist_bench.sh
```

Final trials:

- 19.90508694713038
- 19.892191056735903
- 19.9566154094772
- 19.94446427150992
- 19.939330437271455
- 19.955349126822906

Final median: **19.939330437271455 dec_tps**.

Spread: 0.3%.

Contamination factor: 0.11x.

vs llama.cpp Metal ~60 tps: **33.2%**.

## Mixtral Demo

Result: synthetic-only preview. No Mixtral 8x7B Q4_K_M GGUF was present in
`models/`, and the v1.0.0 Mixtral engine intentionally returns a preview error.

18 GB launch claim status: not demo-ready. Phase 1 provides the expert-cache
foundation, but Phase 2 did not land the real standard-MHA/top-2 MoE forward
path needed for a live Mixtral run.

## Validation Notes

- Build: `cargo build --release --workspace` passed.
- Lib tests: `cargo test --release --workspace --lib` passed.
- Clippy: `cargo clippy --release --workspace -- -D warnings` passed.
- Core parity: existing suites passed except the aggregate run's
  `greedy_64_matches_pinned_hash` check is flaky in the full sweep; the same
  test passed in isolation, and the rest of the suite passed with only that
  single check skipped.
- Smoke: V2-Lite 12-token generation remained sensible English.

## Ship Recommendation

Do not ship v1.0.0 as the full memory-differentiation launch. The perf number
is stable and slightly up (19.5 -> 19.94 coexist), but the Mixtral story is
preview/infrastructure-only. Recommendation: hold launch for v1.0.1, complete
the real Mixtral engine/offload wiring, then run the manager's clean bench and
Mixtral screenshot pass.
