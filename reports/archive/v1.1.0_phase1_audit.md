# v1.1.0 Phase 1 Audit

Date: 2026-05-13

## Summary

Phase 1 should proceed to Phase 2 with a measurement-environment risk flag.
The historical close gate references 22-24+ dec_tps, but this session's
explicit pre-flight anchor is `16.186592750033334` coexist dec_tps, and the
user instruction says to use that measured baseline rather than the historical
20 dec_tps number.

## Measurements

- Pre-flight coexist anchor: `16.186592750033334` dec_tps.
- Phase 1A coexist median: `15.363223818886155` dec_tps, spread `19.0%`.
- Phase 1B temp per-shape coexist median: `16.582778249302553` dec_tps,
  spread `7.0%`.
- Checked-in profile V2-Lite smoke after 1B: `15.67` dec_tps, sensible text.
- Session regression guard: `>=15` dec_tps.

## Hypothesis

The apparent gap against the roadmap's 22+ threshold is environmental, not a
Phase 1 regression. The pre-flight baseline was already 16-17 dec_tps in
coexist mode, and the Phase 1B per-shape profile measured slightly above that
baseline. Phase 1A's lower coexist result had high spread and no DeepSeek
runtime-path change.

## Decision

Proceed to Phase 2. Do not flip the checked-in profile default from `v2` based
on the Phase 1B result because the per-shape profile improved only about
`+2.4%`, below the `+5%` default-flip gate.
