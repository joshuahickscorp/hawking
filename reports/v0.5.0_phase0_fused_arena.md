v0.5.0 phase0 (wedge 0a+0b) outcome: STUCK

Files touched:
- crates/dismantle-core/src/profile.rs: +15 lines (new v0.5.0-fused-arena variant + test updates)
- profiles/deepseek-v2-lite-q4.m3pro18.json: regenerated by autotune (now selects v0.5.0-fused-arena)

New profile selection (post-autotune):
- id: v0.5.0-fused-arena
- moe_schedule: single-kernel
- gpu_buffer_reuse: decode-arena
- gemm_q4_k_schedule: v2
- deterministic_rank: 2

Tests:
- cargo test --workspace --lib: 13 pass
- cargo test --release --tests: all suites pass (incl phase2_moe_block_fused_parity: 7/7 at atol=1e-3)

Smoke generation (32 tokens from "Once upon a time"):
STUCK — single-kernel dispatch runs at ~0.01 tps (100s/token) vs 1.91 tps baseline.
First 200 chars of partial output (killed at 600s alarm after 6 tokens):
  ", a young man named"
Exit code: 142 (SIGALRM — binary never completes, killed by 600s wall timer).
Pre-wedge baseline (indexed-no-pack-one-cb): same prompt exits 0 in <10s (dec_tps=1.91, 8 tokens).

Root cause:
The moe_block_fused_v2lite_indexed_metal kernel passes parity (numerical correctness vs CPU
reference at atol=1e-3) but the runtime dispatch path for moe_schedule="single-kernel" is
not exercising the Metal path. The generate binary produces coherent tokens but at ~200x below
baseline throughput, consistent with CPU fallback or serial un-batched Metal dispatch.
The parity test runs the kernel directly in isolation; the integration path (generate command)
apparently takes a different code route that bypasses or misuses the fast path.

What attended work unblocks:
Inspect the moe_schedule dispatch branch in crates/dismantle-core/src/moe/ (or wherever
"single-kernel" is matched). Verify that the Metal kernel is being enqueued correctly and
that the command buffer is committed and waited on properly. The parity test confirms kernel
correctness; the integration failure points to dispatch infrastructure (cb commit/wait,
buffer aliasing with decode-arena, or a fallback branch triggered at batch=1 decode that
reverts to scalar).
Files to inspect:
  - crates/dismantle-core/src/moe/dispatch.rs (or equivalent dispatch match on moe_schedule)
  - crates/dismantle-core/src/kernels/mod.rs (moe_block_fused_v2lite_indexed_metal entry point)
  - tests/phase2_moe_block_fused_parity.rs (see how the kernel is called in isolation vs generate)

Followups:
- Add a per-token timing log to the generate loop to confirm whether it's CPU fallback vs slow Metal
- Check if decode-arena + single-kernel has a buffer aliasing issue that forces blocking waits
- Compare dispatch call-site in parity test vs generate command to find the divergence
- Consider a micro-smoke: generate --max-new-tokens 1 with --debug-schedule to see which code path fires

Build/tests: clean
Commit: HALT (see git log)

Manager next-step: inspect single-kernel dispatch integration path before re-attempting smoke.
Do NOT re-attempt by lowering the alarm timeout — the kernel is running, just very slowly.
Clean bench irrelevant until the integration hang is resolved.
