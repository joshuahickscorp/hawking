v0.3.6 outcome: LANDED

Harness deliverable:
- tools/bench/clean_bench.sh — 403 lines
- bench_results/v0.3.6/.gitkeep — present
- Script syntax check (bash -n): pass

Profile-toggle verification:
- gemm_q4_k_schedule reference in deepseek_v2.rs: lines 929, 967
  (in moe_expert_matmul_dispatch and moe_expert_pair_matmul_dispatch)
- Confirms simdgroup vs scalar dispatch is profile-driven

Pre-flight gates implemented:
- [x] slm gate (excludes aslmanager)
- [x] Spotlight gate (top non-bench/non-dismantle process < 30% CPU, 10-min wait window)
- [x] Claude-process gate (Claude Helper / Renderer / GPU / claude CLI < 10% CPU)
      NOTE: gate runs BEFORE Spotlight wait loop so it fails fast from executor sessions.
      On macOS, ps -axo comm gives full executable paths; gate matches on path content
      (comm ~ /Claude Helper/ || comm ~ /\/claude$/) rather than on short command name.
- [x] Build sanity (./target/release/dismantle --version)

Trial protocol implemented:
- 3 simd + 3 scalar, alternating (simd_t1, scalar_t1, …, scalar_t3)
- 30s sleep + Spotlight + Claude gate re-check between trials
- Discard rule: decode_ms > 590000 OR completion_tokens < 50
- One retry per discard; abort (ABORTED.txt) on two consecutive discards

Dry-run --gates-only output (from inside executor session):
```
2026-05-03T16:47:00Z Capturing pre-flight top-process snapshot...
103.3 /Applications/Claude.app/.../Claude Helper (Renderer).app/.../Claude Helper (Renderer)
 49.8 /System/Library/PrivateFrameworks/SkyLight.framework/Resources/WindowServer
 25.5 /Applications/Claude.app/.../Claude Helper.app/.../Claude Helper
  3.3 /.../claude.app/Contents/MacOS/claude
2026-05-03T16:47:00Z gate/slm: pass
2026-05-03T16:47:00Z gate/claude: checking for Claude Helper / Renderer / GPU / claude CLI...
FAIL gate/claude: Claude Code is running. Quit Claude Code (Cmd+Q on the desktop app) and re-run this script from a plain terminal.
Offending processes:
 25.5 /Applications/Claude.app/.../Claude Helper.app/.../Claude Helper
103.3 /Applications/Claude.app/.../Claude Helper (Renderer).app/.../Claude Helper (Renderer)
EXIT:1
```

User invocation block (verbatim from script header):
```
# USAGE — run from a plain terminal with Claude Code fully quit
#
#   1. Quit the Claude Code desktop app (Cmd+Q in the menu bar).
#   2. Quit any 'claude' CLI sessions (incl. MASTER_LOOP).
#   3. Open a fresh Terminal.app window.
#   4. cd /Users/scammermike/Downloads/dismantle
#   5. ./tools/bench/clean_bench.sh
#   6. Wait ~30–45 min for all 6 trials. Results appear in
#      bench_results/v0.3.6/. Read summary.md.
#   7. Commit + push the bench_results/v0.3.6/ directory.
#   8. Tell the manager: "v0.3.6 bench results landed".
```

cargo test regression: 13 passed (no regression vs v0.3.4)

Stop conditions encountered: none

Build/tests: cargo test --workspace --lib passed
Commit pushed: (see below)

Next steps (for v0.3.7):
- User runs ./tools/bench/clean_bench.sh from a Claude-quit shell
- bench_results/v0.3.6/summary.md lands with median dec_tps + rubric bucket
- Manager reads results and designs v0.3.7 (next code wedge or
  pivot, depending on rubric outcome)
