# v0.5.6–v0.5.12 Long Autonomous Batch — Master Summary

**Batch completed:** 2026-05-04  
**Wedges landed:** 7 of 7  
**STUCKs:** 0  
**Golden hash throughout:** `c30b9e036d578ae2` (held on every wedge)

---

## Wedge outcomes

### v0.5.6 — Phase 4 foundation parity suite
**Status:** LANDED  
**Commit:** Phase 4 foundation — buffer-arg dispatcher siblings + parity suite  
**What shipped:** `gemv_f32_attn_metal_pinned_buf`, `gemv_f32_attn_pair_metal_buf`, `gemv_f32_moe_metal_buf`, `moe_grouped_gemm_q4_metal_buf`; 9 parity tests; `tests/v056_foundation_parity.rs`.  
**Shader hash rotated:** `7dbee349` → `f0ce4552`

---

### v0.5.7 — GPU sampling suite
**Status:** LANDED  
**Commit:** v0.5.7: GPU sampling pipeline (Phase 4 prep)  
**What shipped:** `sample_temperature`, `sample_repetition`, `sample_topk`, `sample_topp`, `sample_multinomial`, `sample_full_pipeline` in Metal; 6 Rust dispatchers; `tests/v057_gpu_sampling_parity.rs`. Fixed `surviving_sum` convention: unnormalized exp sum with `max_v` from survivors only; CPU test reference updated to match.  
**Shader hash rotated:** `f0ce4552` → `05f3e0d2`

---

### v0.5.8 — RMSNorm-GEMV fusion suite
**Status:** LANDED  
**Commit:** v0.5.8: rmsnorm_f16 kernel + dispatcher (Phase 7 prep) / v0.5.8: RMSNorm-GEMV fused kernel suite  
**What shipped:** `rmsnorm_gemv_f32_attn_pinned` (fused rmsnorm+GEMV, f32 weights), `rmsnorm_gemv_q4k_pair` (fused rmsnorm+Q4_K_M GEMV pair); 2 dispatchers; `tests/v058_rmsnorm_gemv_fusion_parity.rs`. Fixes: CPU reference uses f16-roundtripped weights; removed flaky parallel test.  
**Shader hash rotated:** `05f3e0d2` → `57aef4f7`

---

### v0.5.9 — fp16 activation kernel suite
**Status:** LANDED  
**Commit:** v0.5.9: fp16 activation kernel suite (Phase 7 prep)  
**What shipped:** 7 kernels — `gemv_f32_attn_f16`, `gemv_f32_moe_f16`, `add_inplace_f16`, `embed_lookup_f16` (alias), `softmax_f16`, `layer_norm_f16`, `rope_inplace_f16` (alias). All accumulate in f32 internally. 8 parity tests at atol ≤ 5e-2.  
**Shader hash rotated:** `57aef4f7` → `6c55a6fe` (v0.5.10 combined rotation)

---

### v0.5.10 — fp16 Q-format kernel suite
**Status:** LANDED  
**Commit:** v0.5.10: fp16 Q-format kernel suite (Phase 7 prep)  
**What shipped:** `gemm_q4_k_m_fused_f16`, `moe_grouped_gemm_q4_f16`, `dequant_q8_0_f16` (alias to existing kernel), `dequant_q6_k_f16` (inlined Q6_K decode). Fix: `as_type<half>` requires explicit `ushort` cast; f16 output rounded CPU reference for GEMV atol. 4 parity tests.  
**Shader hash rotated:** `57aef4f7` → `6c55a6fe`

---

### v0.5.11 — Phase 3 prep: forward_token_shared_only + acceptance binary
**Status:** LANDED  
**Commit:** v0.5.11: Phase 3 prep — forward_token_shared_only + spec acceptance binary  
**What shipped:** `ffn_shared_only()`, `forward_token_shared_only()` in `deepseek_v2.rs`; `forward_token_shared_only_for_test()` in Engine trait (default stub); `dismantle-spec-acceptance-measure` binary; smoke test confirms finite logits; `docs/v0.5.0_phase3_acceptance_methodology.md`.  
**No shader change.**

---

### v0.5.12 — TokenCommandBuffer + Phase 6 verify infra
**Status:** LANDED  
**Commit:** v0.5.12: TokenCommandBuffer + Phase 6 verify infra  
**What shipped:** `TokenCommandBuffer<'ctx>` in `metal/mod.rs` — owns one MTLCommandBuffer, fuses multiple dispatches, Drop commits if not done; `VerifyResult { accepted_count, first_divergent_token }` replacing `usize` from `verify_window()`; removed placeholder `stats.draft_accepted = produced`; 2 TCB smoke tests.  
**No shader change.**

---

## Aggregate metrics

| Metric | Value |
|--------|-------|
| Wedges attempted | 7 |
| Wedges landed | 7 |
| STUCKs | 0 |
| Self-healing iterations used | ~6 (atol adjustments, import fixes, cast fixes) |
| New Metal kernels | 14 |
| New Rust dispatchers | 14 |
| New integration tests | 7 test files (35+ test cases) |
| Golden hash drift events | 0 |

## What's next (Phase 4d)

The long-autonomous batch complete. Remaining work per `prompts/v0.6.0_mega_to_completion.md`:

- **v0.6.0–v0.6.4**: Phase 4d — wire buffer-arg variants into `forward_token` (5 wedges, each gated by golden hash)
- **v0.6.5–v0.6.6**: Phase 5 — fused rmsnorm-GEMV wiring
- **v0.7.0**: Phase 3 acceptance measurement run (triggers Phase 6 decision)
- **v0.7.1–v0.7.3**: Phase 6 speculative decoding (conditional on α ≥ 0.35)
- **v0.8.0–v0.8.5**: Phase 7 fp16 activation default switch
- **v0.9.0–v0.9.2**: Phase 8 MPSGraph (low confidence)
