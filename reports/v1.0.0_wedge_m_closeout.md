# Wedge M Closeout

**Session:** sleepy-maxwell-fc16da  
**Date:** 2026-05-06  
**Branch:** claude/sleepy-maxwell-fc16da (HEAD 6ffce2d)

---

## Summary

All 4 planned Wedge C TCB consolidations completed. TCB `commit_and_wait()` budget reduced from ~191 → 109 per token (43% reduction). All 10 parity suites and 18 lib tests pass.

---

## Commits landed

| Commit | Subject | Impact |
|---|---|---|
| dad5c45 | M-1: dispatch site audit (18 TCB cycles → consolidation plan) | Report only |
| b68f903 | M-2: consolidate Wedge C post-loop TCBs (2→1 commits) | –1 commit/token |
| 9309350 | M-3: consolidate attention Phase 3 into β-TCB (27 commits/token saved) | –27/token |
| 96a102e | M-4: consolidate gate GEMV into β-TCB (27 commits/token saved) | –27/token + shader fix |
| 6ffce2d | M-5: fold embed+add_inplace into Phase 1 TCB (27 commits/token saved) | –27/token |

**Total TCB commit reduction (Wedge C): 191 → 109/token (-82/token)**

---

## Consolidation outcomes

| Consolidation | Saved | Status |
|---|---|---|
| C-1: post-loop add_inplace + final-norm → 1 TCB | 1/token | Done |
| C-2: Phase 3 + Mini-TCB β → 1 TCB (×27 layers) | 27/token | Done |
| C-3: gate GEMV folded into C-2 TCB (×27 layers) | 27/token | Done |
| C-4: embed (li=0) / add_inplace (li>0) folded into Phase 1 TCB | 27/token | Done |

Mandatory floor remains 82/token (Phase1 ×27 + Phase2 ×27 + final-norm ×1) — cannot be reduced without GPU-side RoPE and GPU mla_kv_append kernels.

---

## Bonus fix (M-4)

Pre-existing bug in `sample_argmax_f32_metal` (line 1290 of kernels/mod.rs): function was dispatching with `(1,1,1)` threads, but the shader (`sample_argmax_f32` in sample.metal) requires `(256,1,1)` + threadgroup memory allocation per its v0.5.7-A comment. The TCB variant (`sample_argmax_f32_tcb`) was already correct. Fixed: `test_sample_argmax_f32_matches_cpu` now passes.

---

## Bench results (coexist mode, no profile)

**Profile status:** Profile hash mismatch — profile stores `33560f92d0a91929f606a25c` but current shaders compute `ed7a95161d3e6262a6a5a7fa`. Profile was generated at a previous shader state; the hash in the profile is stale. Bench ran without `--kernel-profile`; kernel selection falls back to defaults.

**3 trials (TOKENS=64, no profile):**

| Trial | dec_tps | dispatch_commits_per_token | metal_buffers_created_per_token |
|---|---|---|---|
| 1 | 1.731 | 437.75 | 1385.75 |
| 2 | 1.391 | 437.75 | 1385.75 |
| 3 | 1.773 | 437.75 | 1385.75 |

**Metric note:** `dispatch_commits_per_token` counts `MetalContext::dispatch_threads()` and `dispatch_batch()` calls (old-style counted path only). `TokenCommandBuffer::commit_and_wait()` is NOT counted. Wedge M's savings are in the TCB commit count (uncounted), not `dispatch_commits_per_token`. The 437.75 figure is the old-style dispatch count, which Wedge M does not affect.

**Throughput comparison invalid:** Phase C baseline (2.207 dec_tps) was measured clean (no Claude) + with profile. Coexist + no profile numbers are not comparable.

---

## Follow-ups for attended session

1. **Profile regeneration:** Run `dismantle bench --profile-scan ...` with current binary to regenerate `profiles/deepseek-v2-lite-q4.m3pro18.json` with correct shader hash `ed7a95161d3e6262a6a5a7fa`. This will restore the optimal kernel path and allow clean-bench comparison.

2. **Throughput validation:** Run `tools/bench/clean_bench.sh` (Claude quit) with regenerated profile to get authoritative dec_tps. Expected improvement: 25-45% (eliminating 82 commit_and_wait stalls from the hot path).

3. **Wedge F audit note:** Wedge F's `attention_tcb_inner` (Wedge G changed Phase 1 to read `arena.x_buf` via fused rmsnorm_gemv, but Wedge F residual lives in `wedge_f_x_f16`) may produce stale results for the rmsnorm_gemv kernels. Pre-existing issue, not touched by Wedge M. Needs attended triage.

4. **TCB commit counting:** There is no bench metric for `commit_and_wait()` count. Future attended session could add a TCB commit counter to `MetalContext::Stats` for direct measurement.

---

## Test status

- `cargo build --release --workspace`: PASS  
- `cargo test --release --workspace --lib`: 18/18 PASS  
- `cargo test --release --test phase1_kernel_parity`: 10/10 PASS
