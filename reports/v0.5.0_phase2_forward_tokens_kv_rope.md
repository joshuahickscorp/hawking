v0.5.0 phase2 wedges 2a+2b+2c outcome: LANDED

Files touched:
- crates/dismantle-core/src/model/deepseek_v2.rs: +74 (forward_tokens private method + mla_kv_append associated fn + refactored attention() KV append + Engine trait impl forward_tokens_for_test)
- crates/dismantle-core/src/model/mod.rs: N/A (Engine trait is in engine.rs, not mod.rs)
- crates/dismantle-core/src/engine.rs: +11 (Engine trait method forward_tokens_for_test)
- crates/dismantle-core/src/model/qwen_dense.rs: +18 (trait impl forward_tokens_for_test — loop over forward_token)
- crates/dismantle-core/src/model/qwen_moe.rs: +8 (trait impl forward_tokens_for_test — returns Unimplemented, consistent with rest of stub)
- crates/dismantle-core/src/kernels/mod.rs: +21 (rope_inplace_batch)
- crates/dismantle-core/tests/phase2_foundation_parity.rs: NEW

Approach (Engine trait extension vs pub helpers): Engine trait extended with
`forward_tokens_for_test` — load_engine returns Box<dyn Engine> so trait
dispatch works directly from tests and future callers without unwrapping.
`mla_kv_append` implemented as an associated function (no &mut self receiver)
rather than a method, necessary because attention() holds `cfg = &self.config`
for its entire body; calling a &mut self method would conflict. The static
form `Self::mla_kv_append(&mut self.mla_c_kv, &mut self.mla_k_pe, ...)` uses
Rust split-borrow rules (different fields) to compile cleanly without touching
attention()'s logic.

Validation:
- cargo test --workspace --lib: 18/18 pass (13 dismantle-core + 5 dismantle)
- cargo test --release --tests: 52/52 pass across all integration test suites
- integration_greedy_64: PASS (golden hash unchanged: c30b9e036d578ae2)
- phase2_foundation_parity: 3/3 PASS
  - rope_batch_matches_sequential: PASS
  - rope_batch_empty_is_noop: PASS
  - forward_tokens_shim_returns_n_vectors: PASS (model loaded, 3 logit vectors returned)

Build: clean (0 warnings, 0 errors)
Commit pushed: (see git log)
