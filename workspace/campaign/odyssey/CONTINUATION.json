{
  "objective": "Close the autonomy loop once: an HCLI worker takes a unit to an ACCEPTED terminal and writes a law, so hcli_owned stops being 0 of 25.",
  "active_specimen": "hcli/perception -- the freshly sublated package",
  "why_this_specimen": "It is new, small, and has an obvious untested edge. Evidence here is mechanically producible: a test that is RED before the change and GREEN after, which is exactly what _validate accepts. The previous attempt failed NO_EVIDENCE because the task required judgement it could not evidence in one unit.",
  "active_workunit": "AUTONOMY-2: one red-before-green mutation in hcli/perception",
  "hypothesis": "The machinery works -- wake, compile, spawn, model call all verified. What was missing was a task whose evidence is a passing test rather than a claim.",
  "next_action": "In hcli/perception/store.py, ProjectStore.artifact_path raises ValueError for a digest shorter than 3 characters but does not reject a digest containing a path separator, so a crafted digest could escape the artifacts directory. 1. Write hcli/tests/test_artifact_path_cannot_escape.py asserting that ProjectStore.artifact_path('../../etc/passwd') raises ValueError. 2. Run it and confirm it FAILS (red). 3. Add the guard to artifact_path so the test passes (green). 4. Run the test again and confirm it passes. Report the test file path and both run results as your evidence.",
  "evidence_refs": [
    "hcli/perception/store.py"
  ],
  "open_jobs": [],
  "resource_deps": [
    "a model path in HCLI_MODEL_PATH or --model"
  ],
  "representations_ruled_out": [],
  "written_at": 1788889622
}
