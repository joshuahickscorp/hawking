{
  "objective": "Close the autonomy loop: one accepted unit, hence the first hcli_owned law.",
  "active_specimen": "hcli/perception/store.py",
  "why_this_specimen": "Small and new, with a gap I verified is genuinely RED before the change.",
  "active_workunit": "AUTONOMY-4",
  "hypothesis": "Both prior units died on a HARNESS defect, not a model one: insert/append fragments were compiled standalone and falsely rejected as syntax errors. That is fixed. The previous task was also ill-posed -- its test passed before any change. This one is verified red.",
  "next_action": "ProjectStore.create in hcli/perception/store.py accepts an empty project name, so a project can be created whose name is the empty string. 1. Write hcli/tests/test_project_name_must_not_be_empty.py asserting that ProjectStore.create(tmp_path/'p', '') raises ValueError. 2. Run it. It MUST fail -- the guard does not exist yet. 3. Add the guard using op='insert_after' with old_lines copied VERBATIM from the file (the 'store = cls(root)' line inside create), raising ValueError when the name is empty or whitespace. Do not use replace_file. 4. Run the test again and confirm it passes. Report both runs as your evidence.",
  "evidence_refs": [
    "hcli/perception/store.py"
  ],
  "open_jobs": [],
  "resource_deps": [
    "--model"
  ],
  "representations_ruled_out": [],
  "written_at": 1788892941
}
