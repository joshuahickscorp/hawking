# Q30 null-report archive

The uncalled `q30_activation_null_first_report.py` one-shot reporter was
retired. The current Q30 activation probe and weighted repack remain active;
the null-report implementation is recoverable from Git and its results remain
in the sealed ascent/headless receipts.
