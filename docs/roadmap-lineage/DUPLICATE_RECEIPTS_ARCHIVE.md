# Duplicate receipt archive

The byte-identical aliases `receipts/headless/CONTROL_PLANE_LATENCY.json` and
`receipts/headless/RUNTIME_AB.json` were removed. Their canonical retained
representations are `CONTROL_PLANE_LATENCY_LEDGER.json` and
`RUNTIME_AB.lineage-matched.json`; the checkpoint inventory now names only
those canonical files. No evidence payload was discarded.
