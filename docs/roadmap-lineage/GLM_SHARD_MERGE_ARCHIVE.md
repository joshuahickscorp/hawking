# GLM teacher-forced shard merger archive

The 406-line `tools/condense/merge_glm_teacher_forced_shards.py` producer and
its 153-line producer-only test were retired. No active production path called
the merger; the retained Frankenstein/GLM teacher-forced receipts remain the
evidence boundary, and the live GLM52 streaming/teacher paths are unchanged.
