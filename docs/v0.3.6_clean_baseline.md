# v0.3.6 — clean scalar baseline (post-v0.3.4 attention coalescing)

Captured 2026-05-03 from a Cmd+Q-Claude-desktop terminal session per the
v0.3.6 harness preflight gate. Single trial because v0.3.6's
`completion_tokens` discard rule has a parsing bug (see Open issues).

## Headline

| Config | dec_tps | Δ vs historical (v0.2.2 = 1.861) |
|---|---:|---:|
| scalar (production default) | **2.169** | **+16.5%** |

The +16.5% improvement is attributable primarily to v0.3.4's attention
coalescing (q_a_proj + kv_a_proj into one CommandBatch), which lives in
the fp32 GEMV path and is shared between the scalar and simdgroup
schedules.

## Trace summary (top kernels by total_us, 64-token decode)

```
| rank | kernel             | total_us  | share  | mean_us | count  |
|------|--------------------|----------:|-------:|--------:|-------:|
|  1   | dispatch_batch     | 11,333,319| 51.0%  |   4,172 |  2,716 |
|  2   | gemm_q4_k_m_fused  |  6,554,968| 29.5%  |     459 | 14,280 |
|  3   | rmsnorm            |  1,078,453|  4.9%  |     193 |  5,576 |
|  4   | gemv_f32_attn      |  1,052,330|  4.7%  |     573 |  1,836 |
|  5   | mla_decode_kernel  |    977,325|  4.4%  |     532 |  1,836 |
|  6   | gemv_f16           |    882,798|  4.0%  | 220,699 |      4 |
|  7   | gemv_f32_moe       |    346,770|  1.6%  |     196 |  1,768 |
```

Aggregate dispatch wall: 22.23s. Total decode wall: 29.51s.
Dispatch share of decode: 75.3%.

## v0.3.x trajectory (only commits with clean numbers)

| version | dec_tps (clean) | wedge |
|---|---:|---|
| v0.2.2 | 1.861 | scalar default (revert of v0.2.0 four wedges) |
| v0.3.6 | **2.169** | accumulated v0.3.0–v0.3.4 structural changes |

Intermediate v0.3.0/v0.3.1/v0.3.2/v0.3.3/v0.3.4 numbers were collected
under the contaminated executor session and are not authoritative. The
relevant *clean* delta is v0.2.2 → v0.3.6 = +16.5%, attributable to:

- v0.3.4 q_a/kv_a coalescing — biggest single-wedge contribution
  (gemv_f32_attn count down ~89% per the v0.3.4 report)
- v0.3.3 silu_mul GPU port (lost in the noise of CPU-silu vs GPU-silu
  in scalar; primarily helps simd path)
- v0.3.2 gate+up pair-batched (simd-path-only)

## Open issues blocking next-wedge design

1. **~~simd path 0-token regression~~** — RESOLVED in commit `0be26ed`.
   It was a `clean_bench.sh` parsing bug, not a correctness issue.
   The simdgroup schedule produces 57 valid tokens (model EOS-es a
   few tokens earlier than scalar's 64 because of slight numerical
   drift), but completion_tokens lives at `.results.trial_stats[0].
   completion_tokens` and the harness was reading `.trial_stats[0].`
   (missing the `.results.` prefix). Every trial returned 0 → discard
   rule fired. **simd path is correctness-OK, just slower than scalar.**
2. **~~Bench harness completion_tokens parsing~~** — RESOLVED with
   the same commit `0be26ed`.
3. **gemv_f16 outlier at 220ms mean per call.** 4 calls (the LM head
   at vocab × hidden = ~100k × 2048) take 882ms total = 4% of dispatch
   wall. p99 is 872ms — one call alone is taking nearly a second. A
   proper simdgroup GEMV at this shape should be ~5ms. Tractable wedge
   for a 3-4% perf win. **In flight as v0.3.8.**
4. **MoE hot path is `moe_block_batched_indexed_metal`, not the
   per-expert fallback.** v0.3.7 STUCK on this. The simd full-expert
   fusion was wired into dead code for the production model. Discovery
   wedge **in flight as v0.3.9** to catalogue what actually fires.

## Path to flagship

Current: 2.169 dec_tps. Reference llama.cpp Metal on M3 Pro (b9000):
59.6 dec_tps. Gap: **27.5×**.

Structural CB coalescing is largely drained — the v0.3.x series got
+16.5% from the entire campaign. Closing the rest of the gap requires
real kernel-level work, in approximate order of impact:

- **Q4_K_M GEMV rewrite with proper register tiling** (29.5% of wall):
  the v0.3.0 simdgroup_matrix port was naive and lost. A proper version
  with multi-row threadgroups, register tiling, and reduced barrier
  count is the largest single perf wedge available.
- **Multi-expert fused MoE GEMV**: instead of looping 8 experts × 3
  GEMVs per layer, dispatch one kernel that processes all activated
  experts via z-dim (route_id). Reduces dispatch count and improves
  arithmetic intensity per launch. Note: v0.2.0 attempted this as
  "two-stage MoE" and it regressed −79%; the regression is a kernel
  layout issue, not the structural idea — needs a clean re-attempt.
- **MLA flash attention**: replace mla_decode_kernel (5% of wall, 532μs
  mean) with a proper fused softmax+matmul flash variant.
- **MPSGraph integration**: Apple's graph compiler may give 2-5× on
  large GEMV/GEMM. Speculative; requires plumbing a non-Metal-direct
  path.
- **gemv_f16 LM head simdgroup port**: small (4% of wall) but tractable.
