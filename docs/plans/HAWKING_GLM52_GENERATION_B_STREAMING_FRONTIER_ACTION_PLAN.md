/goal

# HAWKING GLM-5.2 GENERATION B STREAMING FRONTIER
## Finish tabula-rasa closure, freeze the research substrate, run a small aggressive pilot, then restart one fast complete BF16 stream using shard-window rotation, exact teacher capture, complete tensor coverage, sub-0.75 physical candidates, and targeted full-model evaluation

Operate in the authoritative Hawking repository:

```text
/Users/scammermike/Downloads/hawking
```

Expected official parent:

```text
zai-org/GLM-5.2
official BF16 source
immutable revision resolved live
approximately 282 safetensor shards
```

Expected preparatory document:

```text
HAWKING_GLM52_GENERATION_B_TABULA_RASA_PARADIGM_GOAL.md
```

Live Git state, immutable source manifests, evidence seals, process state, storage state, and measured hardware override every expected value below.

This document supersedes the idea that tabula-rasa preparation is the endpoint.

The preparation phase exists only to make the actual restart safe.

The mission after preparation is:

> Run one concrete Generation B GLM streaming campaign from zero logical coverage to 100 percent source and tensor coverage, using a frozen research program, aggressive but bounded pilot science, maximum-safe Xet and compute overlap, exact complete BPW accounting, complete teacher evidence, and a permanent `.gravity` container. Produce scientific transfer, not a public model release.

This is a research rung in the broader model ladder.

It is not yet a ship-ready Hawking model.

Do not stamp, publish, package, advertise, or preserve GLM as a permanent release merely because a complete artifact exists.

The complete GLM artifacts are research instruments used to:

```text
test Gravity
establish the lowest reachable complete BPW
diagnose failure mechanisms
validate the streaming pipeline
exercise the decoder/runtime
transfer stronger priors into the next parent
```

---

# 0. CAMPAIGN ENDPOINTS

## 0.1 Phase P endpoint

The first prerequisite endpoint is:

```text
TABULA_RASA_READY
```

This means:

```text
old invalid run stopped
old Desktop artifact retired
`.gravity` container ABI frozen
decoder correctness closed
packer largest-tensor safe
protected-tensor coverage enforced
teacher payload verification enforced
natural teacher program defined
Generation B storage and controller rehearsed
```

If this state is not already sealed, finish it first.

Do not launch Generation B around an unresolved Tier-0 integrity defect.

## 0.2 First return endpoint

The chat may first return only after:

```text
GENERATION_B_STREAMING_ACTIVE
```

All must be true:

```text
pilot science complete
program frozen
new controller detached
one complete representative window traversed
one teacher capsule reloaded and reproduced
one complete protected-tensor payload sealed
one safe BF16 eviction completed
source coverage advancing
artifact coverage advancing
status and resume state current
all green code pushed
```

## 0.3 Detached controller endpoint

The detached controller continues until:

```text
100 percent official source coverage
100 percent declared tensor coverage
complete research `.gravity` candidates assembled
strongest justified candidate evaluated through earned fidelity
raw BF16 windows evicted
GLM scientific result sealed
next-parent transfer packet sealed
```

## 0.4 No-release endpoint

The final GLM state is:

```text
RESEARCH_RUNG_COMPLETE
```

not:

```text
SHIP_READY
```

No release tag, public model card, distribution bundle, or permanent model-library promotion is authorized.

---

# 1. VERIFY AND CLOSE GENERATION A

Read and verify all prior closure artifacts and the current machine state.

Independently reproduce the two Tier-0 failures that motivated the restart:

```text
protected tensors billed but not physically stored
teacher capsule receipts trusted without opening/verifying payloads
```

Verify the real missing categories:

```text
routers
router controls
normalizations
IndexShare/indexer tensors
embeddings
lm_head
other protected organs
```

At an atomic boundary:

```text
stop old fetch/probe/pack/controller processes through supported controls
verify process-group exit
verify leases released
verify no old files mapped
seal final old-run state
```

Preserve only:

```text
minimal invalid-artifact fixtures
negative-control metrics
coverage ledgers
defect receipts
source lineage
test vectors
```

Delete the exact obsolete Desktop GLM output after:

```text
real-path validation
symlink rejection
MOP exclusion
repository exclusion
process mapping check
exact manifest seal
fixture preservation
```

Record reclaimed bytes.

Create or verify:

```text
GLM52_GENERATION_A_FINAL_STATE.json
GLM52_GENERATION_A_DESKTOP_RETIREMENT.json
```

---

# 2. FREEZE THE GENERATION B SUBSTRATE

Generation B begins only from an integration branch containing all green closure work.

Expected branch:

```text
campaign/glm52-generation-b
```

Integrate and verify:

```text
teacher capture and eviction gating
capsule payload verification
protected/native tensor payload writing
complete tensor coverage gate
largest-tensor-safe k-means
bounded index unpack
decoder safety
stable tensor identities
bounded caches
container integrity
production adapter seam
stream controller rehearsal
```

The `.gravity` extension is permanent:

```text
.gravity
```

The stable container ABI is:

```text
hawking.gravity.container.v1
```

Representation IDs remain versioned and replaceable.

Examples:

```text
glm52.pq.r0.v1
glm52.functional.block.v1
glm52.indexshare.student.v1
glm52.hybrid.doctor.v1
```

The container must not imply that any one codec is permanent.

Freeze:

```text
GRAVITY_CONTAINER_SPEC.md
GRAVITY_CONTAINER_SCHEMA.json
GRAVITY_CONTAINER_TEST_VECTORS.json
GRAVITY_CONTAINER_COMPATIBILITY.json
```

Required decoder status before restart:

```text
CORRECTNESS_COMPLETE_PERFORMANCE_OPEN
```

Final TPS optimization must not block source streaming, but the production call path must exist.

---

# 3. RESEARCH POLICY: GLM IS NOT A RELEASE MODEL

This run deliberately separates:

```text
scientific completeness
runtime completeness
release readiness
```

Generation B aims for scientific and runtime completeness.

It does not claim release readiness.

Do not spend the campaign on:

```text
public packaging
model cards
release-site integration
installer polish
HIDE product UX
distribution mirrors
compatibility promises beyond the frozen container ABI
```

Do spend the campaign on:

```text
exact physical rate
teacher trajectory
failure localization
complete source coverage
complete artifact execution
full-model quality
direct runtime evidence
method transfer to the next parent
```

At final closure, retain only:

```text
one best research artifact
one compact negative/control fixture when scientifically necessary
manifests
teacher-evidence capsule
reports
runtime test fixtures
next-parent transfer packet
```

Do not retain all failed full candidates.

Raw BF16 source windows are always temporary.

Deletion of the final complete GLM research artifact occurs only after the transfer packet is sealed and the user explicitly authorizes release of that artifact.

---

# 4. PILOT SCIENCE: EXPERIMENT BEFORE THE FULL STREAM

Do not run broad representation experiments across all 282 shards.

Most candidate search belongs in a small representative pilot.

However, do not assume that literal shard 0 represents the model.

Shard boundaries are storage boundaries, not architectural boundaries.

Use a dependency-aware pilot covering no more than six representative windows:

```text
P0:
    global tensors, embeddings, first dense layer

P1:
    final dense layer and dense-to-MoE transition

P2:
    first sparse MoE layer and first complete IndexShare group

P3:
    middle sparse/IndexShare group

P4:
    late sparse/IndexShare group

P5:
    final layer, final norm, lm_head, and optional MTP boundary
```

If the official tensor index proves fewer windows cover these roles, use fewer.

If one window contains multiple roles, reuse it.

The pilot may refetch a small window when needed.

The full stream must not repeatedly refetch the complete parent for candidate search.

---

# 5. AGGRESSIVE RATE LAW

The run must remain below one complete physical bit per logical source weight.

Generation B is intentionally more aggressive than prior anchors.

## 5.1 Full-stream candidates

The frozen full-source program may contain at most:

```text
one primary candidate
one aggressive challenger
one bounded negative/control candidate
```

The primary candidate must be:

```text
<= 0.75 complete BPW
```

Do not carry a 0.90 to 0.98 candidate through the whole source merely because it is easier.

A 0.90 to 0.98 row may exist only as a pilot oracle/control when needed to determine whether failure is caused by the representation family or the rate.

## 5.2 Mandatory pilot ladder

Build exact physical pilot artifacts at:

```text
approximately 0.70 to 0.75 BPW
approximately 0.48 to 0.50 BPW
approximately 0.30 to 0.33 BPW
```

Each must have:

```text
exact serialized payload
all headers and alignment billed
all protected tensors billed
runtime tables billed
Doctor state billed
deterministic decode
real teacher-state F0/F1
```

## 5.3 Half-bit policy

The half-bit candidate is mandatory pilot science.

It becomes a complete full-model candidate when:

```text
F1 trajectory remains reachable
F2 propagation is not catastrophically negative
no protected-domain failure is observed
the physical runtime path exists
```

If half-bit fails pilot reachability:

```text
seal the exact causal reason
do not spend a full 1.51-TB traversal reproducing an already-closed failure
```

If half-bit passes:

```text
promote it immediately as the aggressive full-stream challenger
```

## 5.4 One-third policy

Approximately 0.33 BPW is a capability-density boundary probe.

Promote it beyond pilot only if it unexpectedly survives the same frozen gates.

Do not soften the gates to force a lower headline.

---

# 6. REPRESENTATION FAMILIES FOR THE PILOT

The pilot must compare materially different ways of preserving the state trajectory.

Do not reopen unchanged Kimi-negative methods.

Use Kimi evidence as a prior, not a universal law.

## 6.1 Native functional block student

Directly generate:

```text
post-attention state
pre-router state
weighted MoE output
post-block state
```

The BF16 model is a trajectory teacher, not a weight blueprint.

## 6.2 IndexShare-aware attention student

Preserve:

```text
index selection
shared indexer reuse
attention output
context-dependent behavior
```

## 6.3 Hybrid native base plus Doctor

Place a physically serialized, inference-available correction at the first measured causal divergence.

Doctor competes with base representation for every bit and every operation.

## 6.4 Product/vector control

Retain a bounded PQ/R0 control only to compare against the new native-functional direction.

Do not let the control become the selected model by inertia.

## 6.5 Structural allocation

If pilot sensitivity evidence supports it, allocate bits asymmetrically across:

```text
dense layers
attention/indexer
routers
shared expert
routed experts
late layers
final norm/head
```

All experts remain represented unless a separate causal topology experiment earns a change.

---

# 7. PILOT QUALITY AND PROMOTION

Use strictly disjoint:

```text
fit
cross-validation
score
held-out
replication
```

Natural teacher evidence must include:

```text
English
Chinese
mixed language
code
math
reasoning
instruction following
tool/agent formatting
rare tokens
low-router-margin cases
longer-context cases
```

Measure:

```text
attention/IndexShare agreement
attention output
pre-router hidden state
router top-k/margins
shared-expert output
weighted routed-expert output
post-MoE state
post-block state
next-window propagation
short logits
domain and tail behavior
```

Candidate promotion requires:

```text
exact physical rate
complete tensor semantics
positive held-out trajectory result
replication without refit
no severe domain/tail regression
direct runtime support
```

Freeze:

```text
GLM52_GENERATION_B_FROZEN_PROGRAM.json
```

The program includes:

```text
candidate identities
rates
codec versions
teacher memberships
Doctor allocations
promotion thresholds
retirement rules
tensor coverage policy
source-window schedule
runtime ABI
```

After this point:

```text
no broad candidate search during the full stream
```

Only a targeted diagnosis can change the frozen program.

---

# 8. STREAMING PIPELINE: TIGHT SHARD ROTATION

Generation B must implement the most efficient safe rotation possible:

```text
FETCH
-> VERIFY
-> TEACHER_CAPTURE
-> FIT/PACK ALL FROZEN CANDIDATES
-> PROPAGATE
-> COMPLETE COVERAGE SEAL
-> EVICT
```

## 8.1 Dependency windows, not arbitrary shards

Parse the official tensor index and build:

```text
tensor <-> shard <-> layer/organ
```

A dependency window is the smallest complete set needed for one sequential teacher/packing step.

Do not quantize a partial layer merely because one source shard finished first.

## 8.2 Three-stage rotation

Maintain:

```text
N-1:
    hash verification, evidence seal, garbage collection, eviction

N:
    BF16 teacher forward, candidate fitting, packing, propagation

N+1:
    Xet prefetch and source reconstruction
```

Allow an N+2 metadata or speculative prefetch lane only when it increases measured throughput without causing duplicate source bytes, swap growth, disk risk, or heavy-lane regression.

## 8.3 Candidate-inner execution

While BF16 tensors are resident:

```text
capture teacher truth once
fit every frozen candidate
run every required control
serialize every compact payload
measure every propagation metric
```

Then evict the source bytes.

Never download the same window once per candidate.

## 8.4 Complete coverage before eviction

A window is evictable only when:

```text
teacher payload opened and verified
all source tensors have terminal coverage
protected/native payloads physically present
compact payloads physically present
logical and physical ledgers reconcile
all hashes verify
candidate propagation sealed
no queued dependency requires the raw body
```

A receipt or descriptor alone cannot authorize deletion.

## 8.5 Source byte amplification

Track:

```text
network bytes / official source bytes
```

Targets:

```text
ideal:
    <= 1.05x

acceptable:
    <= 1.10x

investigate:
    > 1.10x

pipeline failure:
    > 1.25x without explicit targeted-refetch reason
```

Every refetch is attributed.

---

# 9. XET THROUGHPUT AUTOTUNING

Before the full traversal, run a bounded autotuner across representative shards.

Test:

```text
concurrent files:
    adaptive
    8
    16
    24
    32
    48

range concurrency:
    16
    32
    64

high-performance mode:
    off
    on

prefetch depth:
    1
    2
    3 dependency windows

chunk cache:
    disabled
    minimal
    bounded only when reuse proves valuable
```

Measure:

```text
network throughput
CPU
RAM
swap
disk write amplification
Xet reconstruction amplification
retry rate
teacher/packer slowdown
```

Select the largest useful simultaneous shard/window count.

Do not choose a conservative tiny window when the machine and disk can sustain more.

Do not choose a reckless large window that duplicates data or causes stalls.

Create:

```text
GLM52_GENERATION_B_XET_AUTOTUNE.json
```

---

# 10. PACKER PERFORMANCE AND RESOURCE PRESSURE

Use the device aggressively through useful work.

Do not recreate MPS OOMs or swap thrash merely to display high RAM usage.

## 10.1 Largest tensor

Require:

```text
distance/argmin chunking
one complete accumulation path
bounded MPS working set
no full [N,K] allocation
no leaked wired memory
fault cleanup
checkpoint resume
```

Run the real vocabulary/head scale proof before the full stream reaches those organs.

## 10.2 Dynamic worker controller

Autotune:

```text
pack workers
probe workers
CPU threads
MPS fitting workers
Xet workers
evidence compression workers
```

Increase concurrency while:

```text
sustained packs/hour improves
memory pressure is green
swap growth is bounded
wired memory is normal
thermals are green
disk reserve is green
```

Back off automatically on:

```text
MPS OOM
wired-memory anomaly
swap acceleration
thermal warning
pack-rate regression
source-lane starvation
```

Measure sustained rates over meaningful intervals.

Do not extrapolate from a short burst.

## 10.3 Survey sampling

Production candidate fitting remains exhaustive for every admitted tensor.

Only discarded diagnostic rungs may be sampled.

Every skipped measurement is marked:

```text
sampled_out
```

not:

```text
failed
```

---

# 11. STORAGE POLICY

Use generation-specific TCC-safe roots:

```text
state:
~/Library/Application Support/Hawking/GLM52/<revision>/generation_b/state/

transient source:
~/Library/Caches/Hawking/GLM52/<revision>/generation_b/source/

research artifacts:
~/Library/Application Support/Hawking/Research/GLM-5.2/<revision>/generation_b/
```

No permanent output on Desktop.

## 11.1 Emergency and operational reserves

Emergency free-space floor:

```text
5 GiB
```

Operational reserve is dynamic:

```text
max(
    emergency floor,
    two largest atomic source writes,
    largest candidate write,
    next checkpoint write,
    Xet scratch requirement,
    projected compact-artifact growth
)
```

The pipeline may use all remaining safe disk.

## 11.2 One-copy law

Do not retain:

```text
a complete cache view
and
a complete materialized view
```

of the same BF16 source.

## 11.3 Rolling garbage collection

After every sealed window:

```text
evict verified raw BF16 dependencies
remove unreferenced Xet scratch
remove superseded pilot payloads
compact logs
remove failed full-candidate shards once aggregate evidence is sealed
```

Retain:

```text
active window
prefetched window
current primary candidate
current aggressive challenger
minimal control
required teacher evidence
rollback
```

---

# 12. AUTONOMOUS CONTROLLER

Launch one durable Generation B controller using:

```text
launchd
caffeinate
exclusive heavy lease
fresh heartbeat
atomic checkpoints
fault recovery
resume after reboot where supported
```

State machine:

```text
PRECHECK
ADMIT_SOURCE
PILOT
FREEZE_PROGRAM
PLAN_WINDOWS
FETCH
VERIFY
TEACHER_CAPTURE
FIT
PACK
PROPAGATE
COVERAGE_SEAL
EVICT
CHECKPOINT
ASSEMBLE
VERIFY_ARTIFACT
RUN_COMPACT
TARGETED_REFETCH
FINALIZE_SCIENCE
COMPLETE
BLOCKED
```

Every transition is:

```text
atomic
idempotent
restart-safe
one-use claimed
status-producing
```

Telegram is optional.

Atomic local status is mandatory.

Maintain:

```text
GLM52_GENERATION_B_STATUS.md
GLM52_GENERATION_B_STATUS.json
GLM52_GENERATION_B_WINDOW_LEDGER.jsonl
```

Report:

```text
source coverage
tensor coverage
teacher coverage
candidate artifact coverage
current window
stage timings
network throughput
pack throughput
source-byte amplification
free disk
RAM/swap/wired memory
controller PID/lease/heartbeat
fault/recovery
ETA
```

---

# 13. FULL-SOURCE TRAVERSAL POLICY

Once the program is frozen, the full stream is a production-like scientific execution, not an open-ended search.

During the full stream:

```text
do not add a fourth candidate
do not change rate targets casually
do not refit from held-out failures
do not rerun pilot grids on every shard
do not pause for cosmetic tooling
```

Do:

```text
collect layer/window trajectory data
retire a candidate early when a preregistered impossibility condition fires
continue the surviving candidates
reuse all teacher evidence
keep source and artifact coverage synchronized
```

If every candidate fails early:

```text
continue only the minimum source traversal required to establish the parent-specific law
or
freeze a new program only after a formal causal diagnosis
```

Do not stream 1.51 TB merely to manufacture a complete but scientifically dead artifact.

---

# 14. TARGETED REFETCH, NOT FULL RESTART

After assembling a complete candidate, diagnose failures by:

```text
layer
organ
domain
route margin
IndexShare group
expert class
context length
```

Rank source windows by expected information gain and repair value.

Refetch only affected dependency windows.

Every refetch requires:

```text
exact source bytes
reason existing evidence is insufficient
candidate treatment
expected recovery
candidate payload cost
```

Replace only affected `.gravity` shards.

Do not repeat the complete source stream for each Doctor revision.

---

# 15. DOCTOR AND CAUSAL SCIENCE

Doctor is a dynamic physical allocation, not a posthoc slogan.

It may act at:

```text
post-attention
IndexShare boundary
pre-router
shared/routed expert output
post-MoE
post-block
logits
```

Required causal swaps where valid:

```text
teacher IndexShare plus compact value path
compact IndexShare plus teacher value path
teacher attention output plus compact MoE
compact attention plus teacher MoE
teacher router plus compact experts
compact router plus teacher expert outputs
teacher weighted MoE output
teacher hidden reset before first divergence
teacher hidden reset after first divergence
```

Every Doctor bit and operation is billed.

A teacher intervention is an oracle, not a deployable candidate.

The objective is to identify whether GLM is:

```text
INDEXER_BOUND
ATTENTION_BOUND
ROUTER_BOUND
EXPERT_FUNCTION_BOUND
STATE_TRAJECTORY_BOUND
MIXED
```

---

# 16. RUNTIME AND TPS LANE

Runtime work proceeds concurrently, but it must not delay or mutate the frozen scientific stream.

The `.gravity` decoder must execute the representation natively.

Priority:

```text
bounded packed-index decode
stable tensor registry
shared on-chip lookup-linear
2D split-chunk fallback
native packed-width execution
real production adapter
complete MoE layer
attention 3 command buffers to 1
one replayable token graph
```

The measured diagnostic findings to reproduce before relying on them are:

```text
lookup-linear:
    large measured advantage when the table remains on-chip and is shared

2D split:
    large measured advantage over serial chunk loops

old 35.9x:
    invalid under matched dense baselines
```

True token TPS is measured only when the path includes:

```text
embedding
normalization
attention/IndexShare
router
shared/routed experts
final norm
lm_head
sampling
KV update
```

Partial stack timing remains labeled partial.

Runtime status does not block the BF16 stream once correctness and ABI are stable.

---

# 17. GENERATION B FULL ARTIFACTS

The full run may produce:

```text
primary <=0.75 BPW research artifact
half-bit research artifact when promoted
bounded control artifact when required
```

Every complete artifact must verify:

```text
100 percent source tensor disposition
embeddings present
lm_head present
routers present
normalizations present
IndexShare tensors present
all expert tensors present
complete physical bytes
complete BPW
all section hashes
deterministic decode
no BF16 runtime dependency
```

Artifacts are marked:

```text
RESEARCH_ONLY_NOT_RELEASED
```

Do not add them to the permanent shipped model registry.

---

# 18. FULL-MODEL QUALITY

For the strongest complete candidate, evaluate:

```text
NLL/perplexity
logit divergence
top-k agreement
deterministic generation
coding
math
reasoning
instruction following
tool/agent formatting
Chinese
English
mixed language
rare tokens
long-context retrieval
long-context synthesis
```

Use context ladder:

```text
2K
8K
32K
128K when earned
```

A candidate that fails complete-model quality may still yield a useful causal law.

It is not a successful model.

---

# 19. GLOBAL MODEL-LADDER TRANSFER

GLM is one rung.

At closure, produce:

```text
GLM52_NEXT_PARENT_TRANSFER.md
GLOBAL_GRAVITY_LADDER_UPDATE.json
```

State:

```text
methods to inherit
methods never to rerun unchanged
best complete rate
half-bit status
first causal divergence
Doctor/base prior
runtime grammar
streaming throughput
source-byte amplification
adapter requirements
recommended first pilot windows for the next parent
```

The next parent should begin from the strongest transferable methodology.

Do not spend days polishing GLM after the transferable result is already sealed.

No Hawking model is stamped ship-ready until a later global release campaign explicitly verifies:

```text
cross-parent representation stability
complete runtime
quality
artifact ABI
deployment
HIDE integration
support policy
```

---

# 20. REQUIRED ARTIFACTS

Produce at minimum:

```text
GLM52_GENERATION_A_FINAL_STATE.json
GLM52_GENERATION_A_DESKTOP_RETIREMENT.json
GRAVITY_CONTAINER_SPEC.md
GRAVITY_CONTAINER_SCHEMA.json
GLM52_GENERATION_B_COMMIT_INVENTORY.json
GLM52_GENERATION_B_PILOT_PROGRAM.json
GLM52_GENERATION_B_PILOT_RESULTS.json
GLM52_GENERATION_B_FROZEN_PROGRAM.json
GLM52_GENERATION_B_XET_AUTOTUNE.json
GLM52_GENERATION_B_CAPTURE_PROGRAM.json
GLM52_TENSOR_COVERAGE_POLICY.json
GLM52_PACKER_CLOSURE.json
GLM52_GENERATION_B_STATUS.json
GLM52_GENERATION_B_WINDOW_LEDGER.jsonl
GLM52_GENERATION_B_SOURCE_COVERAGE.json
GLM52_GENERATION_B_ARTIFACT_MANIFEST.json
GLM52_GENERATION_B_COMPLETE_COVERAGE.json
GLM52_GENERATION_B_QUALITY.json
GLM52_GENERATION_B_GRAVITY_FINAL.md
GLM52_GENERATION_B_GRAVITY_FINAL.json
GLM52_NEXT_PARENT_TRANSFER.md
GLOBAL_GRAVITY_LADDER_UPDATE.json
GLM52_ROLLBACK.json
```

---

# 21. GIT AND RELEASE DISCIPLINE

Commit and push green campaign milestones:

```text
Generation A closure
container/coverage fixes
packer closure
natural capture program
pilot science
frozen program
Xet autotune
controller launch
coverage checkpoints
complete research artifacts
full-model evaluation
transfer packet
```

Do not merge to main without explicit user approval.

Do not create a model-release tag.

A scientific closure tag is allowed:

```text
hawking-glm52-generation-b-research
```

only after final evidence verifies.

---

# 22. FIRST RETURN GATE

Do not return from the launch chat until:

```text
1. TABULA_RASA_READY is verified
2. Generation A is stopped
3. obsolete Desktop output is retired
4. pilot windows are complete
5. <=0.75 candidate is selected
6. exact half-bit pilot is complete
7. frozen program is sealed
8. Xet autotune is sealed
9. Generation B controller is detached
10. first complete protected-tensor window is sealed
11. first teacher capsule reload passes
12. first safe BF16 eviction passes
13. source coverage advances
14. artifact coverage advances
15. resident disk remains bounded
16. heartbeat/lease/status are healthy
17. all green work is pushed
```

Required first return:

```text
endpoint: GENERATION_B_STREAMING_ACTIVE
source revision:
pilot windows:
primary candidate:
primary complete BPW:
half-bit status:
control:
Xet configuration:
maximum shards/windows in flight:
first complete window:
teacher capsule:
coverage seal:
source coverage:
artifact coverage:
source-byte amplification:
controller PID/lease/heartbeat:
disk/RAM/swap:
commits pushed:
next autonomous action:
```

---

# 23. FINAL CONTROLLER OUTCOME

Seal one:

```text
OUTCOME_A:
    complete <=0.75 GLM research artifact survives full-model quality

OUTCOME_B:
    complete approximately 0.50 GLM research artifact survives full-model quality

OUTCOME_C:
    complete research artifacts exist, but tested sub-0.75 region closes negatively

OUTCOME_D:
    native functional representation becomes the next-parent architecture despite no complete GLM pass

OUTCOME_E:
    exact reproducible technical blocker prevents completion after all unaffected work is sealed
```

The final report must distinguish:

```text
physical success
trajectory success
full-model success
runtime success
release readiness
```

Release readiness remains false unless a later dedicated release campaign changes it.

---

# FINAL DIRECTIVE

Finish preparation, then launch.

Do not repeat the old moving experiment.

Do not use literal shard zero as a universal model proxy.

Use a small architecture-representative pilot, freeze one aggressive program, and stream the parent once.

Download the largest safe next dependency window while the current window is being captured, quantized, propagated, and sealed.

For every resident BF16 byte:

```text
extract every frozen scientific result
produce every surviving compact payload
verify every protected tensor
then delete the raw byte
```

Keep the primary complete artifact below 0.75 BPW.

Force an exact half-bit pilot.

Promote half-bit when the state trajectory remains reachable.

Use approximately one-third BPW to map the boundary, not to manufacture a headline.

Do not save GLM as a shipped model.

Use GLM to strengthen Gravity, `.gravity`, the streaming engine, Doctor, and the runtime, then transfer the result aggressively into the next parent.

> Generation B is complete when Hawking has traversed the official BF16 parent once through a frozen, complete, high-throughput scientific pipeline and extracted the strongest transferable sub-bit law, not when another model folder is merely stamped “done.”
