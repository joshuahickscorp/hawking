# Phase History Archive

Historical haul specs, manifests, blocked docs, closeouts, and old
planning notes live here so the repository root stays readable.

Active development should start from the root `README.md`, `ROADMAP.md`,
`ARCHITECTURE.md`, and the code under `crates/`. These files are retained
only as audit trail and context for old autonomous-haul workflows.
