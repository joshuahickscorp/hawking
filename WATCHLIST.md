# WATCHLIST.md — lapping-condition spec

> Spec source: `docs/plans/studio_maximization_2026_06_27.md` §20.11 / §19.8 #9 ("lapping alarm").
> One row per threat, each with an explicit **invalidation trigger** — what, if it ships, kills
> part of Hawking's wedge. Public self-obsolescence monitoring builds unusual trust: re-run this
> table on the stated cadence and update the README the moment a gap closes.

| watched | what would lap us | check cadence | invalidates |
|---|---|---|---|
| MLX-QAT | a one-command "condense + recover into MLX format" path | monthly | the Apple-native recovery half |
| Pruna | a first-class LLM-on-Apple-Silicon path | monthly | the BYO-condenser category corner |
| PonyExl3 | adds even light LoRA recovery to trellis-on-Metal | monthly | the "trellis + Metal + recovery" combo |
| EXL3 / QTIP | an official Metal port | monthly | trellis-on-Mac serving |
| Unsloth | a recovery (gradient) step on top of dynamic bits | monthly | the recovery-depth claim |
| BitNet / vendor-QAT | a downloadable native-ternary model > ~2.4B, BYO | monthly | the "BYO → 2-bit" gap (§13b) |

## How to run the alarm

1. Once a month, check each watched project's releases/changelog for the "what would lap us" event.
2. If an event fires, open a `FAILURES.md`-style note (tag `baseline-beat-us` if it beats a card),
   update the README scoreboard, and if it hits a §20.1 "kills the wedge" condition, trigger the
   §14 portfolio reframe.
3. Record the check date here even when nothing changed (the cadence itself is the evidence).

_Last checked: (not yet run — seeded 2026-06-27 during pre-Studio scaffolding.)_
