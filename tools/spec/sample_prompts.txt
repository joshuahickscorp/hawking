# Default sample prompts for `python3 tools/spec/run.py ngram`
# One prompt per line. Lines starting with # are comments.
# These are diverse prompts spanning code, prose, and Q&A to get a realistic
# oracle estimate across workload types.

fn fibonacci(n: u64) -> u64 {
Tell me about the history of machine learning and neural networks.
impl Iterator for MyStruct { type Item = u32; fn next(&mut self) -> Option<Self::Item> {
The quick brown fox jumps over the lazy dog. The dog did not seem to mind.
def merge_sort(arr): if len(arr) <= 1: return arr
What are the main differences between transformers and recurrent neural networks?
fn main() { let v: Vec<i32> = (0..100).collect(); println!("{:?}", v); }
In the beginning there was darkness. Then someone said let there be light and there was.
SELECT * FROM users WHERE created_at > '2024-01-01' ORDER BY id DESC LIMIT 100;
The transformer architecture introduced self-attention which allows the model to
