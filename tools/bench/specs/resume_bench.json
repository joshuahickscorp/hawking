{
  "schema": "hawking.lab.experiment.v1",
  "id": "resume_bench",
  "title": "bench:resume_bench.sh",
  "artifact_dir": "artifacts/runs/resume_bench",
  "pause_flag": "artifacts/runs/PAUSE",
  "resume_flag": "artifacts/runs/RESUME",
  "meta": {
    "source_entrypoint": "tools/bench/resume_bench.sh",
    "framework_retired": "bench_shell_harness",
    "condensed": "g1-declarative-body-v1"
  },
  "stages": [
    {
      "id": "body",
      "shell": "export LAB_REPO_ROOT=\"$(pwd)\"\n#!/usr/bin/env bash\n# harness-invoked: cwd is repo root\n# Resume a paused pipeline. See tools/bench/pause_bench.sh.\nset -euo pipefail\ncd \"${LAB_REPO_ROOT:-$(pwd)}\"\ntouch artifacts/runs/RESUME\necho \"resume signal sent.\"\necho \"(pipeline checks every 10s; will clear PAUSE + RESUME and continue.)\"\n",
      "on_fail": "abort",
      "skip_if_done": false
    }
  ]
}
