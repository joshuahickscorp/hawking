{
  "schema": "hawking.lab.experiment.v1",
  "id": "batch_aggregate_bench",
  "title": "bench:batch_aggregate_bench.sh",
  "artifact_dir": "artifacts/runs/batch_aggregate_bench",
  "pause_flag": "artifacts/runs/PAUSE",
  "resume_flag": "artifacts/runs/RESUME",
  "meta": {
    "source_entrypoint": "tools/bench/batch_aggregate_bench.sh",
    "framework_retired": "bench_shell_harness",
    "condensed": "g1-declarative-body-v1"
  },
  "stages": [
    {
      "id": "body",
      "shell": "export LAB_REPO_ROOT=\"$(pwd)\"\n#!/usr/bin/env bash\n# harness-invoked: cwd is repo root\n# Aggregate-tps for continuous-batching multi-seq decode (B=1/4/8).\n# The SPEEDUP RATIO is contamination-robust (valid with the agent open); the\n# ABSOLUTE tps needs a clean room. Auto-detected by clean_bench_queue.sh.\nset -uo pipefail\ncd \"${LAB_REPO_ROOT:-$(pwd)}\"\nexec cargo test --release -p hawking-core --test multiseq_aggregate_bench -- --ignored --nocapture\n",
      "on_fail": "abort",
      "skip_if_done": false
    }
  ]
}
