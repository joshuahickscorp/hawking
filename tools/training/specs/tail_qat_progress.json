{
  "schema": "hawking.lab.experiment.v1",
  "id": "tail_qat_progress",
  "title": "training:tail_qat_progress.sh",
  "artifact_dir": "artifacts/runs/tail_qat_progress",
  "pause_flag": "artifacts/runs/PAUSE",
  "resume_flag": "artifacts/runs/RESUME",
  "meta": {
    "source_entrypoint": "tools/training/tail_qat_progress.sh",
    "framework_retired": "training_shell_harness",
    "condensed": "g1-declarative-body-v1"
  },
  "stages": [
    {
      "id": "body",
      "shell": "export LAB_REPO_ROOT=\"$(pwd)\"\n#!/usr/bin/env bash\n# harness-invoked: cwd is repo root\nset -euo pipefail\n\nROOT=\"$(cd \"${LAB_REPO_ROOT:-$(pwd)}\" && pwd)\"\ncd \"$ROOT\"\n\nPY=\"${PYTHON:-python3}\"\nRUN_DIR=\"${QAT_RUN_DIR:-artifacts/lowbit_rwkv7/runs/g1_ffn_ternary_last8}\"\nINTERVAL=\"${QAT_CHECKPOINT_INTERVAL:-5}\"\nREFRESH_SECONDS=\"${QAT_REFRESH_SECONDS:-0}\"\n\nif [[ \"${QAT_CLEAR:-1}\" == \"1\" ]] && command -v clear >/dev/null 2>&1; then\n  clear\nfi\n\nexec \"$PY\" tools/training/rwkv7_progress.py \\\n  --run-dir \"$RUN_DIR\" \\\n  --next-checkpoint \\\n  --checkpoint-interval \"$INTERVAL\" \\\n  --follow \\\n  --refresh-seconds \"$REFRESH_SECONDS\" \\\n  \"$@\"\n",
      "on_fail": "abort",
      "skip_if_done": false
    }
  ]
}
