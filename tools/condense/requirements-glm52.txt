# Immutable-control-plane runtime for the GLM-5.2 BF16/Xet campaign.
# Install inside an isolated Python 3.12 environment.  The tokenizer pin is
# intentional: Transformers 5.14.1 accepts >=0.22.0,<=0.23.0, and 0.23.0 has
# no stable PyPI release as of the 2026-07-21 admission.
huggingface_hub==1.24.0
hf_xet==1.5.2
transformers==5.14.1
safetensors==0.8.0
tokenizers==0.22.2
# Direct numerical dependencies used by the adapter/reference/parity tools.
# They are pinned here rather than inherited from a system site-packages tree.
numpy==2.2.6
torch==2.6.0
# Exact acceptance-test runner used to rebuild and verify the sealed evidence.
pytest==9.1.0
# Ed25519 role separation for the durable notification outbox.  The journal
# receives verifier-only keys for sender/operator authority; private signing
# keys never enter the controller or journal verifier objects.
cryptography==43.0.1

# Fully pinned transitive closure observed in the isolated Python 3.12.6
# acceptance environment.  Keeping these here prevents a later resolver run
# from silently changing the sealed reference implementation underneath the
# same direct-dependency versions.
annotated-doc==0.0.4
anyio==4.14.2
certifi==2026.6.17
click==8.4.2
cffi==1.17.1
filelock==3.32.0
fsspec==2026.6.0
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.18
iniconfig==2.3.0
Jinja2==3.1.6
markdown-it-py==4.2.0
MarkupSafe==3.0.3
mdurl==0.1.2
mpmath==1.3.0
networkx==3.6.1
packaging==26.2
pip==26.1.2
pluggy==1.6.0
pycparser==2.22
Pygments==2.20.0
PyYAML==6.0.3
regex==2026.7.19
rich==15.0.0
setuptools==83.0.0
shellingham==1.5.4
sympy==1.13.1
tqdm==4.69.0
typer==0.27.0
typing_extensions==4.16.0
