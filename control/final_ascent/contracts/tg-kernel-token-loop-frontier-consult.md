# Temporal Gravity kernel and token-loop frontier consultation

Produce an independent, evidence-driven implementation frontier for reaching
TG2, TG1, and below without weakening capability or Numeric Parity V2.1.
This task is read-only. Do not edit the repository, read real model bodies,
launch heavy work, touch MOP, or claim unmeasured speed.

## Inspect

Inspect current source, tests, and receipts for:

- one-time verification and persistent tensor registry;
- native packed-width decode;
- resident activations, routing, KV, and compact MLA state;
- BF16/final head, GPU logits/top-k/sampling;
- routed expert waves/table execution;
- attention/IndexShare fusion;
- command-buffer collapse and ICB replay;
- complete-token graph and persistent causal loop;
- profiler coverage and attribution;
- default-off flags, rejected optimizations, and known parity failures;
- cold/warm `BASE_TRUE_TPS`, TTFT, prefill, bytes/ops/CB per token,
  memory/swap/thermals, and 2K/8K/32K evidence.

Separate proof on a capable GLM artifact from fixture, Llama, refused-artifact,
projection, and microbenchmark evidence.

## Required output

Return a ranked table of every remaining material cost center with:

- source location and current execution path;
- authoritative evidence and confidence;
- correctness/parity dependency;
- expected latency/TPS mechanism, not an invented speedup number;
- implementation size and overlap risk;
- source-body-free prototype or microbenchmark possible now;
- capable-provider measurement required later;
- kill condition and rollback;
- whether it advances TG20/TG10/TG5/TG2/TG1/below-TG1.

Identify at least:

1. the three cheapest code changes likely to reduce physical command buffers or
   per-token CPU/GPU synchronization;
2. the three largest likely flagship bottlenecks;
3. the exact prerequisites for a replayable whole-token graph;
4. the exact prerequisites and viability test for a persistent GPU causal loop;
5. compact 2K/8K/32K KV/state choices that fit measured host limits;
6. kernel fusion candidates that preserve exact decisions;
7. operations that must remain unfused due to parity or dynamic routing;
8. profiler blind spots that prevent trustworthy prioritization.

For each cheap item, write a bounded implementation contract with authorized
files, fake/fixture gate, microbenchmark, parity gate, integration gate, and
flag/default policy. For each expensive item, write a preregistered experiment
with a smallest decisive scope and stop rule.

## Final synthesis

Provide:

- a dependency DAG from current state to TG2/TG1/below-TG1;
- a parallelization schedule distinguishing cheap CPU/light work from the one
  authoritative heavy lane;
- the next three Grok implementation tasks that can start immediately;
- the exact flagship benchmark contract that must wait for a capable,
  hash-approved provider;
- explicit rejection of stale, refused-provider, simulated, or fixture-only
  evidence as a product TPS claim.
