# Independent Ramanujan migration, forge, and Q0-Q6 audit

## Role

Read-only audit. Do not edit files. Reconcile the current embedded `ramanujan/`
work with the directive requiring a separate fully local repository at
`/Users/scammermike/Downloads/ramanujan`.

## Inputs

Inspect current HEAD, relevant commits/worktrees, and all Ramanujan receipts,
especially:

- `ramanujan/RAMANUJAN_GOVERNANCE_STATUS.json`
- `ramanujan/SMALL_SYSTEM_TRAINING_STATUS.json`
- corpus generation/freeze/contamination/scale-up receipts
- training receipts and held-out metrics
- Q0 container build/replay receipts
- Odyssey substrate/fence receipts
- roles, Ledger, Tribunal, stores, retrieval, search, cognition, governance,
  offline recovery, and environment lock

Treat dirty worktrees as unintegrated. Treat a directory scaffold as no more than
a scaffold. The capable Math-Frozen Director does not exist and
`RAMANUJAN_RESEARCH_AUTHORIZED` must remain false.

## Required assessment

1. Inventory exactly what is committed, tested, fixture-only, simulated,
   incomplete, or absent for migration, F0-F9, cognition/search/governance,
   seven stores, governed roles, Ledger/Tribunal, and Q0-Q6.
2. Verify claims around the 16,188-item corpora, split/contamination seals,
   retriever/value convergence, formalizer/prover/repair non-convergence, Lean
   checking, and Q0 network-none replay. State test commands and whether evidence
   is non-skipped.
3. Determine what can be migrated now without copying Hawking history or giant
   bodies, and define the immutable read-only artifact binding contract.
4. State which activities are preparatory engineering versus the true
   open-problem campaign. Preserve the research fence.
5. Identify gaps in offline install, replay, rollback, hidden evaluation,
   multi-day rehearsal, adversarial governance/tool/retrieval tests, and the
   human-readiness packet.

Return a dependency-ordered bounded worklist, exact files/tests, migration
boundary, promotion gates, and remaining external dependencies. Do not create the
separate repository, train against refused artifacts, touch MOP, or flip any
fence during this audit.
