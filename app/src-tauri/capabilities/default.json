{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "HIDE webview capabilities: core APIs plus the native folder picker for Add folder.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
