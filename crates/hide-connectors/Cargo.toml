[package]
name = "hide-connectors"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "HIDE YOU connector ABI and registry: typed least-privilege connectors, account handles, write effects + receipts, connector-scoped memory, real revocation (model-free, fixture-backed)"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
blake3 = { workspace = true }

[dev-dependencies]
tempfile = "3"
serde_json = { workspace = true }

[features]
default = []
