[package]
name = "hawking-events"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "Canonical Hawking/HIDE event model: one durable Event authority, deprecation adapters for superseded models, category kind registry, and export for HAWKING_CANONICAL_EVENTS.json"

[dependencies]
hide-core = { path = "../hide-core" }
hide-protocol = { path = "../hide-protocol" }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
tokio = { workspace = true }
