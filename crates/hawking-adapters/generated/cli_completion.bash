# Generated by hawking-adapters-codegen — do not hand-edit.
# Source: eval "$(hawking-adapters-codegen --print-bash-completion)" or source this file.
_hawking_complete() {
  local cur prev
  COMPREPLY=()
  cur="${COMP_WORDS[COMP_CWORD]}"
  prev="${COMP_WORDS[COMP_CWORD-1]}"
  local cmds="serve generate tokenize bench autotune doctor profile-rank stats version adapters adapters-list adapters-show adapters-validate"
  local profiles="default fast race efficient exact"
  local families="deepseek deepseek deepseek2 deepseek_v2 gemma gemma gemma2 glm glm glm52 glm_moe_dsa glm-5.2 kimi kimi kimi_k2 kimi_k26 moonshot llama llama llama2 llama3 llama3.2 minimax minimax minimax_m3 minimax-m3 mistral_mixtral mistral mixtral mistral_mixtral phi phi phi3 qwen qwen qwen2 qwen2moe qwen-moe qwen3 state_space state_space rwkv7 rwkv mamba2 mamba"
  local grades="DECLARED SOURCE_HEADER_VALIDATED SYNTHETIC_PARITY REAL_TENSOR_DECODE SMALL_REAL_CHECKPOINT FULL_PARENT_VALIDATED PRODUCTION"

  if [[ ${COMP_CWORD} -eq 1 ]]; then
    COMPREPLY=( $(compgen -W "${cmds}" -- "${cur}") )
    return 0
  fi
  case "${prev}" in
    --profile)
      COMPREPLY=( $(compgen -W "${profiles}" -- "${cur}") )
      return 0
      ;;
    show|adapters-show)
      COMPREPLY=( $(compgen -W "${families}" -- "${cur}") )
      return 0
      ;;
    --weights|--gravity|--kernel-profile|--hardware-profile|--profile-json)
      COMPREPLY=( $(compgen -f -- "${cur}") )
      return 0
      ;;
  esac
  case "${COMP_WORDS[1]}" in
    adapters)
      if [[ ${COMP_CWORD} -eq 2 ]]; then
        COMPREPLY=( $(compgen -W "list show validate" -- "${cur}") )
      elif [[ ${COMP_WORDS[2]} == show ]]; then
        COMPREPLY=( $(compgen -W "${families}" -- "${cur}") )
      fi
      ;;
    *)
      COMPREPLY=( $(compgen -W "--profile --weights --gravity --help" -- "${cur}") )
      ;;
  esac
}
complete -F _hawking_complete hawking
