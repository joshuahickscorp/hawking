[package]
name = "hide-you"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "HIDE YOU surface: governed swarms, projects, and typed cross-surface handoffs (claim-only capsules; model-free fixture provider)"

[dependencies]
# No hide-core runtime dep: SurfacePermissionSet / SurfaceCapability mirror
# hide_core::automation::{PermissionSet, JobCapability} structurally, without
# pulling the automation engine or kernel. Pattern source is documented in
# HIDE_YOU_SWARM_CONTRACT.json.
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
blake3 = { workspace = true }
ulid = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
